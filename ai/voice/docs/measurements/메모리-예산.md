# 음성 파이프라인 RAM 예산 · 실측 보고

> 대상 코드: [`ai/voice/`](../../) · Jira: S15P11A301-69, S15P11A301-119, S15P11A301-157 · 갱신: 2026-07-30
> 상태: **단계 1(로컬 LLM·GMS 단독 구동) 실측 완료** · 단계 2·3(통합 구동) 측정 대기
>
> 단계 2·3 측정 절차와 자동 집계 도구는 [`../README.md` §9-5](../README.md)를 따른다.
>
> 용어: [`../README.md` §13 용어집](../README.md#13-용어집)

---

## 1. 한눈에 보는 결론

**Jetson Orin Nano 8GB에서 음성 파이프라인 단독 실측 결과:**

| 핵심 수치 | 값 |
|---|---|
| 로컬 LLM 보고 순간 피크 RAM | **5.62GB / 7.4GB** |
| GMS 음성 세션 피크 RAM | **4102MB / 7620MB** |
| GMS 구성 피크 시 여유 | **3518MB** |

**팀에 공유할 결론:**

1. Orin Nano의 8GB는 **CPU·GPU 통합 메모리**다. 로컬 LLM(qwen2.5:3b) 포함 피크가 5.62GB로, **비전(YOLO)·SLAM·Nav2 몫이 ~1.9GB뿐**이라 통합 시 부족할 가능성이 높았다.
2. **배포 리스크 발견:** qwen2.5:3b는 여유 RAM이 있어 보여도 **page cache가 차 있으면 GPU 로드 실패(OOM)**. 상세는 §3.
3. → **이 실측을 근거로 LLM을 GMS API 호출로 전환하기로 팀 결정(2026-07-24).**
   초기 모델은 `gpt-5-nano`였으며 Jira 118 정량 비교 후 `gpt-5.4-mini`로 변경했다.
   젯슨에서 LLM RAM이 사라진 GMS 구성은 2026-07-30 실측에서
   **4102MB 피크(3518MB 여유)**였다. GUI·PulseAudio가 동작 중인 2670MB
   베이스라인을 포함한 값이다. 이전 예상치 ~3.3GB는 이 실측으로 대체됐다.
   네트워크 단절이 확인되면 신규 STT 대화를 시작하지 않으며, STT 완료 뒤 GMS 호출만 실패한
   경우에는 명세 33-8 키워드 폴백(LLM RAM 0)으로 대응한다.
   상세 측정 조건은 §2-3에 기록했다.

---

## 2. 실측 결과 (단계 1 — 단독 구동)

### 2-1. 측정 환경

| 항목 | 값 |
|---|---|
| 장비 / OS | Jetson Orin Nano 8GB · JetPack 6.2.1 (L4T R36.4.7) · CUDA 12.6 |
| 실행 상태 | **헤드리스**(GUI 종료) · 전원 15W · jetson_clocks off |
| 파이프라인 구성 | VAD(Silero) + STT(faster-whisper small, **CPU**/int8) + LLM(qwen2.5:3b, GPU/ollama) |
| TTS | **미로드** (사전녹음 방식 전제) |
| 스왑 | ~11.7GB |
| 측정 도구 | tegrastats 1초 로깅 (`RAM used/total`, 총 7.4GB 표기 기준) |
| 코드 | commit `bda9efd` · `config: device=cpu compute=int8 stt=small llm=qwen2.5:3b` |
| 측정일 | 2026-07-24 |

> ⚠️ **이 표는 측정 당시 구성이다. 현행과 다르다.**
>
> - **LLM이 GMS API 호출로 바뀌어 젯슨 LLM RAM이 0이 됐다.** 위 표의 `qwen2.5:3b`는
>   피크 5.62GB였고 OOM 위험 때문에 전환했다. 이 절의 수치를 현행 예산으로 읽으면
>   **과대평가**다.
> - **STT는 로컬 `faster-whisper small`로 확정됐다**(2026-08-01, S15P11A301-198 재편에서
>   클라우드 전환 취소). 이 항목은 바뀌지 않았으므로 **재측정 사유가 없다.**
> - `TTS 미로드 (사전녹음 방식 전제)`는 여전히 참이다. 안내 음성은 승인된 WAV 재생이며
>   젯슨에 TTS 모델을 올리지 않는다.
>
> 즉 **STT·VAD·TTS 부분은 유효하고 LLM 부분만 무효**다. 통합(비전+SLAM 동시) 실측은
> 여전히 미완이며 그것이 남은 유일한 재측정 대상이다.

> STT가 CPU인 이유: ARM64용 CTranslate2에 CUDA 빌드가 없음. GPU STT는 whisper.cpp(CUDA) 전환으로 추후 검토.

### 2-2. RAM 사용량

| 상태 | 실측 | (사전 예상) |
|---|---|---|
| idle 베이스라인 (헤드리스) | **~1.8GB** | — |
| VAD + STT 상주, LLM 미로드 | **~3.1–3.3GB** | 2.9–4.3GB |
| **보고 피크 — LLM(3b) 로드** | **5.62GB** | 5.0–6.5GB |
| 여유 (7.4 − 피크) | **~1.9GB** | — |

- 실측은 사전 예상 범위와 일치했다 (예상 근거는 부록 A).
- TTS를 아예 안 올렸는데도 피크가 5.62GB → 통합 여유 확보의 관건은 **LLM 크기**다.

### 2-3. GMS 구성 단독 실측 (2026-07-30)

| 항목 | 값 |
|---|---|
| 장비 / 전원 | Jetson Orin Nano 8GB · 15W mode 0 · `jetson_clocks` 미적용 |
| 코드 / 구성 | `e36f910` · `device=cpu compute=int8 jetson=True stt=small llm=gpt-5.4-mini` |
| 입출력 | Logitech BRIO 100 · TS-BTS25-2-D Bluetooth A2DP |
| 측정 | 시나리오 A 실발화 세션 · `tegrastats --interval 1000` |
| 시작 / 최저 / 피크 RAM | **2670MB / 2568MB / 4102MB** |
| 피크 시 여유 | **3518MB / 7620MB** |
| 세션 E2E | **76.8초** |

GMS 호출은 외부에서 수행되므로 로컬 LLM 메모리가 없고, 같은 보드의 로컬 3b
구성 피크 5.62GB보다 약 1.6GB 낮았다. 이전 ~3.3GB 예상보다 절대 사용량이 높은
이유는 이번 측정이 GUI·PulseAudio·Bluetooth를 포함한 2670MB 베이스라인에서
시작했기 때문이다. 통합 예산에는 절대 피크 4102MB를 보수적으로 사용한다.

### 2-4. 미측정 항목

| 항목 | 왜 남았나 |
|---|---|
| GMS LLM 왕복 지연 · triage 일관성 | bench 재실행 (GMS_KEY 설정 후) |
| STT 지연 · triage 정확도 | 테스트 wav 샘플이 개발 PC에 있음 — 이관 후 측정 |
| **통합 구동 (비전+SLAM 동시)** | **가장 중요.** 팀원과 일정 잡아 §4 진행 |

> §2-2의 5.62GB 실측(로컬 3b 구성)은 재현 대상이 아니라 **GMS 전환 결정의 근거 기록**으로 보존한다.
> 명세 33장의 실측값 기록처(TBD-AUD-002, 부록 H)에도 이 수치를 기입할 것.

---

## 3. ⚠️ 발견: qwen2.5:3b는 page cache 압박 시 GPU 로드 실패

**증상.** 동일 구성에서 LLM 로드가 다음 에러로 실패:

```
cudaMalloc failed: out of memory
failed to allocate CUDA0 buffer of size 1923955712 (1.79GB)
```

**원인.** 당시 메모리 상태는 `free 1.3GB / buff-cache 4.2GB / available 5.0GB`.
"available"은 5GB지만, **젯슨의 CUDA(nvgpu) 할당기는 page cache를 자동 회수하지 못한다.**
그래서 진짜 free(1.3GB)가 요구량(1.79GB)보다 작으면 — 캐시가 아무리 많아도 — 할당이 실패한다.
일반 CPU malloc과 다른 젯슨 특유의 거동이다.

**확인.** `sudo sh -c 'sync; echo 3 > /proc/sys/vm/drop_caches'`로 캐시를 비워 free를 5.1GB로
올리자 정상 로드됨. GUI 종료(헤드리스 전환)만으로는 해소되지 않았다.

**함의.** 장시간 운용으로 캐시가 차오르면 현장에서 LLM 로드가 실패할 수 있다 = **배포 리스크.**

**대응 (택1 이상):**
1. LLM 로드 직전 `drop_caches` 실행 (런처 스크립트에 포함)
2. `keep_alive`로 LLM 상주 유지 → 재로드 자체를 회피 (단, 상주 RAM 부담)
3. 1.5b급 다운시프트 → 요구 버퍼가 ~1GB로 줄어 실패 확률 자체가 감소

---

## 4. 남은 측정: 통합 구동 (단계 2·3)

### 4-1. 안전 게이트 — 순서를 건너뛰지 말 것

이 코드는 모터를 제어하지 않지만(관제 보고는 현재 시뮬 print), 통합 시 **OOM 킬러가 주행·안전
노드를 죽일 수 있다.** 주행 중에 그 일이 벌어지면 위험하므로, 반드시 아래 순서로 진행한다.

| 단계 | 내용 | 로봇 상태 | 통과 조건 |
|---|---|---|---|
| 1 ✅ | 음성 단독 벤치 | 정지 | 완료 (§2) |
| 2 | 비전+SLAM+음성 **동시** RAM 측정 | **바퀴 띄움/정지** | 총 RAM 8GB 안 + 여유 확보, OOM·스로틀 없음 |
| 3 | 실제 주행 중 동시 구동 | 주행 | **단계 2 통과 시에만** + 물리 E-Stop 확인 |

게이트 미통과 시 → §5 다운시프트 적용 후 해당 단계부터 재측정.

### 4-2. 측정 템플릿 (팀원과 함께 채우기)

측정: `tegrastats --interval 1000 | tee 로그파일` + 음성 벤치는 [`evaluation/pipeline_bench.py`](../../evaluation/pipeline_bench.py).

| 동시 구동 조합 | 로봇 | 총 RAM | 여유 | 음성 E2E | 비전 FPS | OOM/스로틀 | 판정 |
|---|---|---|---|---|---|---|---|
| 비전(YOLO)만 | 정지 | | | — | 기준 ____ | | |
| 비전 + SLAM | 정지 | | | — | | | |
| 비전 + SLAM + **음성 보고** | 정지 | | | | | | |
| + Nav2 자율주행 [단계 3] | 주행 | | | | | | |

### 4-3. GMS 전환 전후 실측 비교

| 구성 | 피크 RAM | 최소 available RAM | 종료 후 RAM | 피크 Swap | 측정 상태 |
|---|---:|---:|---:|---:|---|
| 로컬 qwen2.5:3b | **5,620MB** | 미측정 | 미측정 | 미측정 | 과거 실측 |
| GMS gpt-5.4-mini 음성 단독 | 측정 예정 | 측정 예정 | 측정 예정 | 측정 예정 | Jira 119 |
| 비전+SLAM+GMS 음성 | 측정 예정 | 측정 예정 | 측정 예정 | 측정 예정 | Jira 119 |

GMS 구성 실측 후 아래 식으로 로컬 LLM 대비 RAM 절감 효과를 계산한다.

```text
절감량(MB) = 5,620 - GMS 피크 RAM
절감률(%) = 절감량 / 5,620 × 100
```

전원 모드·GUI·백그라운드 서비스가 기존 측정과 다르면 직접 A/B 비교가 아니라
과거 기준 대비 참고 비교로 표시한다.

---

## 5. 다운시프트 가이드 (통합 시 부족하면 위에서부터 적용)

| 순위 | 조치 | 효과 | 트레이드오프 |
|---|---|---|---|
| 1 | **TTS 사전녹음 확정** (MeloTTS 미탑재 유지) | 상주 −1.3~2.0GB | 없음 (안내 문구는 고정) |
| 2 | LLM `keep_alive` 축소 → idle 시 언로드 | idle RAM 회수 | 보고 시 재로드 지연 + §3 리스크 |
| 3 | **LLM 경량화** 3b → 1.5b급(Q4) | 피크 약 −1GB | 일관성·정확도 재검증 필요 |
| 4 | STT small → base | 소폭 | 저SNR 강건성 하락 — 최후수단 |
| 5 | 규칙 기반 파싱으로 LLM 대체 (명세 33장 fallback) | LLM RAM 전부 | 자유발화 이해 포기 |

적용 후에는 §4-2를 다시 측정해 근거를 남긴다.

---

## 부록 A. 사전 예상치 (실측 전 산정 근거 — 참고용)

실측 전 모델 파라미터 수·양자화 기준으로 산정했던 구성요소별 예상치. §2-2에서 실측과 대조 완료.

| 구성요소 | 예상 RAM | 로드 시점 |
|---|---|---|
| torch + CUDA 컨텍스트 | ~0.4–0.7GB | 시작 시 1회 |
| Python·numpy·librosa 등 | ~0.3–0.5GB | 시작 시 |
| Silero VAD | ~0.1GB | 상주 |
| faster-whisper small (int8) | ~0.8–1.0GB | 상주 |
| MeloTTS (KR) | ~1.3–2.0GB | (미탑재 — 탑재 시 상주) |
| ollama qwen2.5:3b (Q4) | ~2.0–2.4GB | 보고 중에만 |
| ollama qwen2.5:1.5b (Q4) | ~1.0GB | 다운시프트 후보 |

- 개발 PC(x86/float16)와 젯슨(ARM64/int8, 통합메모리)은 메모리 회계가 다르므로 PC 측정값으로 대체 불가.
- `sentinel_voice/pipeline.py`는 VAD·STT(·TTS 탑재 시 TTS까지)를 import 시점에 상주 로드한다. "순차 실행으로 피크를 낮춘다"는 LLM(ollama)에만 해당.
