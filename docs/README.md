---
title: "재난 지역 자율 탐사 UGV - Sentinel UGV"
team: "역삼역역무실관제센터"
version: "v1.1"
status: "통합 설계 확정본"
date: "2026-07-27"
---

# 재난 지역 자율 탐사 UGV - Sentinel UGV

> 통합 프로젝트 명세서 및 개발 운영 계획

| **항목**           | **내용**                                                 |
|--------------------|----------------------------------------------------------|
| **팀명**           | 역삼역역무실관제센터                                     |
| **문서 버전**      | v1.1 - 장간 충돌 해소 및 조건부 Pose·음성 하드웨어 확정본  |
| **작성 기준일**    | 2026-07-24                                               |
| **개발 종료 목표** | 2026-08-07                                            |
| **프로젝트 유형**  | 온디바이스 AIoT 기반 재난 탐사 무인 지상 차량(UGV)       |
| **문서 상태**      | 시스템 구조 확정, 실제 부품 정격·배선·튜닝값은 As-Built 단계 TBD |

> **문서 사용 목적** 본 문서는 기획서, 시스템 명세서, 하드웨어 설계 기준, 소프트웨어 인터페이스 명세, 테스트 계획, 일정 및 위험 관리 계획을 하나로 통합한 프로젝트 종합 문서이다. 팀원 간 개발 기준을 통일하고, 기술 선택 근거와 변경 이력을 발표 및 포트폴리오에 활용하는 것을 목적으로 한다.

> **문서 구성 안내** 본 명세서는 역할별 파일과 TBD 대장으로 분리해 관리하지만, **장 번호(1~38장)와 부록(A~L)이 전역으로 연속되는 하나의 문서(v1.1)** 이다. 문서 버전·변경 이력·결정 기준은 이 README에서만 관리하며, 각 분리 파일은 자기 버전을 따로 두지 않는다. 아래 [문서 구성](#문서-구성)의 매핑을 따른다.

# 문서 관리
| **버전** | **일자**   | **상태**  | **주요 변경 내용**                                                    |
|----------|------------|-----------|-----------------------------------------------------------------------|
| v0.1     | 2026-07 초 | 아이디어  | 재난 지역 탐사 로봇 콘셉트 및 Jetson 중심 구조 검토                   |
| v0.5     | 2026-07 중 | 설계      | 짐벌, ROS2, SLAM, Nav2, AWS 관제 구조 검토                            |
| v0.8     | 2026-07-17 | 정책 확정 | 자율 탐사, 수동 조종, 스트리밍, 데이터 저장, 장애 대응 정책 확정      |
| v0.9     | 2026-07-17 | 개발 착수 | 전체 프로젝트 종합 명세서 작성. 모터/전원/짐벌 세부 부품은 TBD로 관리 |
| v1.0-rc1 | 2026-07-22 | 통합 검수 | 1~38장 통합, Jetson+STM32, 피해자 접근·음성·다중 인원, MQTT·이벤트 녹화·보안·인수 시험 반영 |
| v1.0-rc2 | 2026-07-23 | 구동 정정 | 구동계 정정 시도. 실제 donor platform 반영은 rc3에서 재검수 |
| v1.0-rc3 | 2026-07-23 | 대화 재검수 | BMW M7 베이스·RS540·추가 학습 YOLO·STT-LLM-TTS·실측 센서 상태 반영, 폐기 자료 의존 제거 |
| v1.1     | 2026-07-24 | 충돌 해소 확정 | 요약부·상세부 충돌 13건 팀 결정 반영(API 31장 기준, LiDAR 짐벌 탑재, TF 표준화, PAUSED 경유 수동 전환, AVAILABLE 상태 통일, 3주 일정 재편 등). 조건부 Pose 파이프라인, 블루투스 스피커+BRIO 내장 마이크, STT-LLM-TTS 온디바이스 확정. TBD 대장 부록 H로 단일화. 요약부 구조 재편: 상세 장과 중복되는 6·8~14·16장을 핵심 요약+규범 포인터로 압축하고 독자별 읽기 경로 추가. 역할별 6개 파일 + TBD 대장으로 문서 분리 |

| v1.1-r1  | 2026-07-27 | 실측 정정 | v1.1 내 정정(버전 식별자는 v1.1 유지). 32장을 S15P11A301-62 확정 계약·PoC-A 실측과 정합하게 정정. 카메라 단일 오픈을 `usb_cam` raw_mjpeg 패스스루 + 압축 토픽 분기로 교체, 15 FPS → 30 FPS 확정, NVENC 미존재로 `x264enc`(CPU) 확정(HW 인코더 전제 폐기), `nvv4l2decoder mjpeg=1` 확정·`nvjpegdec` 기각 명시, 저장 매체 microSD 실태와 `usb_cam` stamp 클럭 특성 기록, VID-11~13 추가. 변경 파일: `05-통신-서버-영상.md` |
| v1.1-r2  | 2026-07-27 | 제어기 변경 | 저수준 제어기를 STM32에서 ESP32로 변경. 유선 USB Serial, ESP-IDF watchdog·태스크 분리, 부팅 안전 출력, DHT11·전방 초음파의 ESP32 수집 및 독립 근거리 정지 원칙을 반영. 정확한 ESP32 보드 모델과 핀맵은 TBD. 변경 파일: `README.md`, `01~06`, `TBD.md`, `scripts/docs/split-integrated-spec.ps1` |
| v1.1-r3  | 2026-07-29 | AI 문서 정리 | 객체탐지 Jetson 실행 runbook을 25.7장에 반영하고, detection 세부 문서를 `docs/ai/detection/`으로 이동. 변경 파일: `README.md`, `04-자율주행-AI.md`, `docs/ai/README.md`, `docs/ai/detection/*`, `ai/detection/AGENTS.md` |
| v1.1-r4  | 2026-07-30 | 하드웨어 스택 확정 | 짐벌을 프로젝트에서 완전히 제외(카메라·LiDAR 차체 고정 마운트로 전환)하고, 배터리(모터용 유아전동차 내장 12V + Jetson용 대용량 보조배터리 분리 급전), 구동/조향 모터(RS540 FD-12V RPM14000 좌·우 각 1개 + WW MOTOR RS380SP-12V RPM5500 조향 1개, BTS7960 드라이버 3개), 저수준 제어기(ESP32 WROOM-32를 모터 제어용·센서 제어용 2개로 이원화, 각각 Jetson과 별도 USB Serial 연결), 센서(HC-SR04 초음파 1개, DHT-11 온습도 1개, MT6701 엔코더 3개)를 확정 반영. 짐벌 제외로 FR-024·CAL-08/09·`/gimbal/state`·gimbal TF 체인은 폐기 처리. 변경 파일: `README.md`, `01~06`, `TBD.md` |
| v1.1-r5  | 2026-08-01 | 구동계 차동 구동 전환 | 조향 모터(WW MOTOR RS380SP-12V)와 조향되는 앞바퀴를 제거하고, 프레임 앞부분에 수동 캐스터 바퀴 2개를 부착해 후륜과 높이를 맞췄다. 후륜 좌·우 RS540 모터 2개만으로 전진·후진·조향·제자리 회전을 수행하는 **차동 구동(differential drive)**으로 전환. 이에 따라 BTS7960·MT6701을 각각 3개→2개로 감축하고, 전륜 Ackermann 조향·최소 회전반경 제약·조향각 프로토콜 필드(`target_steering_mdeg` 등)·`STEERING_ENCODER_FAULT`·TBD-HW-008을 폐기했다. Nav2는 최소 회전반경 제약이 없는 차동 구동 로봇으로 재설정(NavFn/Smac 2D + DWB, rotation shim 활성화). 변경 파일: `README.md`, `01~06`, `TBD.md` |

| **v2.0** | **2026-08-04** | **구현 정합** | 명세 전체(6204줄·38장)를 리포 실제 상태와 대조해 정정(S15P11A301-239, 238 통합). 남은 기간이 짧아 **명세를 현재 구현에 맞추는 방향**으로 결정했고, 폐기·미구현을 감추지 않고 표기한다. **정책 변경 3건**: ① 배터리 기반 안전·종료 기능 폐기(14.6 — 전압·전류 센서 미장착이라 계측 경로가 없다. 종료 조건이 Frontier 소진과 7분·운영자 종료 둘로 줄어든다), ② 인증을 MVP 범위 밖으로 확정(36-4 — `/api/**`가 `permitAll()`이고 로그인 화면·`users` 테이블이 없다. 경계는 EC2 SG 허용 IP와 CORS 이며 Control Session 은 인증이 아니다), ③ TBD-NAV-001 Nav2 채택 확정(24.1 — Regulated Pure Pursuit + rotation shim, `enable_nav2` 기본 false). **신설**: 8.4 ROS 그래프 격리, 8.5 foxglove_bridge 시각화 경로, 8.6 관제 웹 실시간 지도(v1.1의 "실시간 격자 계약이 없다" 전제가 깨졌다), 23.6 복귀 3단(빵조각 역추적을 2단으로), 25.3 카메라 역할 경계(단안이라 기하에 쓸 수 없다), 03장 캐스터 슬립이 오도메트리 yaw 를 갉아먹는 영향, 04장 3차 통합 FPS 실측(5.80 → 10.80, 목표 15 미달). **정정**: 8.2 노드 표에 구현 상태 열(명세가 이름 붙인 노드 중 절반이 미구현, 6개는 실제 이름이 다르다)과 `/cmd_vel` 소비자 부재 갭 명시, REST API 표를 27.4 단일 규범으로 통합(31-7 중복 제거, `/path` 폐기, 지도 계통 3개 추가), 관제 화면 페이지 4개 → 2개와 레이아웃 제거 항목 6개의 근거, 28장 제목에서 "조이스틱 UX" 제거·STOMP 조종 채널 3개 미사용, 13장 미생성 테이블 3개(`users`·`interaction_sessions`·`interaction_turns`), 37-3 systemd 단일 진입점(TBD-OPS-001 문장 무효), 38-3 에 선행 부재로 막힌 요구사항 8개, 14.3 필수 점검에서 점검 불가 항목 제거, TBD-CAL-002 신설(트랙폭·바퀴 지름 — 즉시). **담당 위임**: 33-5 "전부 온디바이스 확정"이 구현과 다르다(LLM 이 GMS 클라우드) — 프라이버시·오프라인 정책이 걸려 있어 음성 담당이 정정한다. 변경 파일: `README.md`, `01`, `02`, `03`, `04`, `05`, `06`, `TBD.md` |

> **v2.0의 원칙: 미구현을 미구현으로 적는다.** v1.1까지는 설계 의도를 규범으로 적어
> 두었고 그것이 구현과 벌어졌다. 명세가 "구현한다"고 적고 있는데 실제로 없으면, 통합
> 검사에서 "왜 안 되나"를 매번 다시 조사하게 되고 최악의 경우 보안처럼 **없는 보호를
> 있다고 믿는다.** 그래서 v2.0은 각 항목에 구현 상태를 붙이고, 폐기한 것은 폐기로,
> 안 하기로 한 것은 안 한다고 적는다.
>
> 자율주행 23·24장의 **설계 결정**(Nav2 채택 근거, 복귀 3단, 카메라 역할 경계)은
> S15P11A301-238에서 별도로 반영한다. v2.0은 현재 상태 정합까지다.

| v2.0-r1 | 2026-08-04 | 파일 분리 | `04-자율주행-AI.md`를 둘로 분리(S15P11A301-245). 자율주행 장(8·14·22·23·24·26)은 `04-자율주행.md`, AI 장(9·25·30·33)은 신설 `07-AI-탐지-음성.md`. **내용 변경 없음 — 이동만.** AI(객체탐지·음성)는 거의 완료된 참조 문서이고 자율주행은 착수 단계의 작업 문서라, 한 파일에 두면 자율주행 변경마다 완료된 장이 diff 에 섞인다. 장 번호는 전역이므로 번호 참조는 유효하며, 파일명 참조는 리포 전수 갱신(`ai/detection`·`ai/voice`·`docs/ai`). 변경 파일: `README.md`, `04-자율주행.md`, `07-AI-탐지-음성.md`(신설), `ai/`·`docs/ai/` 링크 |

| v2.0-r2 | 2026-08-04 | 주행 스택 구현 반영 | 자율주행 구현 진행분을 반영(S15P11A301-234·235·236·244). 8.2 노드 표에서 `vehicle_kinematics`·`nav2_stack`·`ekf_node` 를 구현으로, 8.1 파이프라인에 EKF 편입. **24.1 신설**: planner/controller 역할 구분과 Nav2 구성 확정(NavFn, rotation shim→RPP, `allow_unknown=false` 가 23.3 도달성 판정의 근거, costmap 2개, `/cmd_vel_nav` remap, 시연 시작 위치 20cm 클리어런스). **23.2 신설**: EKF 융합 — 입력 성분 선택 근거(엔코더 `vx` 만·IMU `vyaw` 만, 슬립은 계통 오차라 공분산 가중으로 안 되고 아예 뺀다), odom TF **3자 배타표**(222 의 2자를 확장), 정지 실측(EKF 30Hz, gyro 잡음 분산 5.1e-05, **잔여 bias −4.85°/분**), 장착 후 검증 3단계. 22.2 센서 표에 IMU 90Hz·엔코더 50Hz 실측. TBD-HW-012 모델·배선 확정(MPU6050 I2C 0x68), TBD-NAV-001 구성 확정. 변경 파일: `README.md`, `04-자율주행.md`, `TBD.md` |

| v2.0-r3 | 2026-08-04 | 환경 센서 판정 | S15P11A301-214. 22.2 에 **환경값 판정 임계** 절 신설 — 명세가 임계값을 두지 않아 여기서 확정했다(습도 상한 85 → **80**: 데이터시트 상한 90 에서 오차 ±5 를 뺀 값. 종전 85 는 경고 구간이 오차 폭과 같았다). **포화 처리**를 도입했다 — DHT11 은 습도 90% 를 넘으면 못 읽으므로 임계값만 두면 범위 밖이 침묵한다. 값이 범위 끝이면 `≥90` 부등호와 "측정 한계" 안내를 함께 낸다. 포화 자체가 경고이며, 습도 하한(≤22%)은 경고 조건(>80) 밖이라 이 절 없이는 건조 극단이 조용히 지나간다. 22.2 DHT11 행에 유효 범위·실패 시 발행 건너뛰기 규칙 기재. 11장 레이아웃 제거 표에 **속도·각속도 폐기** 추가(실측값은 오지만 관제자 행동을 바꾸지 않는다 — 배터리·방위각과 같은 기준. DB·임무 이력에는 남긴다). 변경 파일: `README.md`, `04-자율주행.md`, `05-통신-서버-영상.md` |

| v2.0-r5 | 2026-08-04 | 환경 fault 발화 확인 | S15P11A301-258(r4 는 256 이 쓴다). 22.2 의 「**`ENVIRONMENT_SENSOR_FAULT`(임계 3)는 뜨지 않는다**」를 정정했다 — 그 문장은 측정 조건(센서 장착·간헐 실패 18.5%, 연속 최대 1회)에서만 사실인데 "이 플래그는 안 뜬다" 로 읽혔다. 없는 보호를 있다고 믿게 하는 종류의 오류다(v2.0 의 36-4 와 같다). 같은 날 **센서 분리 상태에서 정상 발화를 확인**했고 조건별 표로 바꿨다: 간헐 실패 → 0·0.41Hz 발행 유지 / 분리 → 1·`DEGRADED`·발행 중단. 분리 중에도 같은 보드 IMU 90.6Hz·CRC 0·프레임 유실 0 이라 fault 가 DHT11 경로만 짚었다는 것, 화면에서 값이 사라지는 것이 **설계대로**라는 것(낡은 값이 새 측정으로 보이지 않게), 주행은 `DEGRADED` 허용이라 탐사가 계속된다는 것을 적었다. 플래그가 **sticky 가 아니라 성공 읽기 1회에 해제**되므로 복구에 재기동이 불필요하고, 값이 1 로 남아 있다는 것은 임계 이후 성공 읽기가 없었다는 뜻이라 간헐 실패와 완전 불통을 이 한 값으로 구별할 수 있다. 변경 파일: `README.md`, `04-자율주행.md` |

| v2.0-r6 | 2026-08-05 | 안전 체인 구현·순서 통일 | S15P11A301-237. 체인을 구현하고(`sentinel_safety` 의 `command_mux`·`safety_gate`, nav2 두 층은 `safety.yaml` 파라미터만) **네 곳에 서로 다르게 적혀 있던 순서·토픽명을 통일**했다. 순서는 `command_mux → velocity_smoother → collision_monitor → safety_gate` 이며 34-7 이 규범이다 — smoother 를 뒤에 두면 급정지가 `max_decel` 에 걸려 천천히 멈춘다. 최종 토픽 이름이 문서마다 `/cmd_vel_safe`(03장)·`/cmd_vel`(8.2 `command_mux` 행)·`/cmd_vel_final`(8.2 `safety_gate` 행) 셋이었고 실제는 `/cmd_vel` 하나다(`/cmd_vel_safe` 는 collision_monitor 출력). 24.1 mermaid 는 수동 명령을 collision_monitor **뒤**에 합류시켜 수동 조작이 충돌 방지를 건너뛰게 그려져 있었다. **24.1 에 구현 확정 절 신설**: 관통 실측 4단 19.995~19.999Hz·값 보존·`/cmd_vel` 발행자 1개, 상류 단절 시 0 을 20.000Hz 로 **계속** 발행(침묵과 정지의 구별), nav2 두 층이 lifecycle 이라 `lifecycle_manager_safety` 가 필요하고 이름이 nav2 쪽과 달라야 하며 CPU 포화 경합 때문에 3초 지연이 필요한 것(지연 충분성은 **저부하에서만** 확인), `base_shift_correction: False` 의 근거와 그 대가 1.75cm 계산, `/scan` 낡음을 collision_monitor 가 건너뛰므로 게이트가 따로 보는 것. 8.2 표에서 `vehicle_kinematics`(234) 가 **아직 미구현으로 남아 있던 것**도 정정했다(v2.0-r2 가 고쳤다고 적었으나 실제로는 안 고쳐져 있었다). 22.4 의 「TF 끊김은 237 이 함께 잡는다」는 **약속이 이행되지 않았음을 명시**로 바꿨다 — 게이트의 정지 조건 넷에 TF 가 없고 `/scan` 은 계속 오므로 collision_monitor 도 못 막는다. 06장 FR-004·FR-011 을 "미구현" 에서 "구현·관통 확인, 실차 계측 미실시" 로, FR-003 의 「`/cmd_vel` 소비자 없음」을 정정. 변경 파일: `README.md`, `03-제어-캘리브레이션.md`, `04-자율주행.md`, `06-테스트-보안-운영.md` |

| v2.0-r7 | 2026-08-05 | 보호정지 중계 구현 | S15P11A301-237(r6 의 누락 보완). r6 이 안전 체인을 구현했다고 적었으나 **명세 03-276 이 요구하는 `protective_stop` → `STOP_COMMAND` 중계가 빠져 있었다** — `safety_gate` 는 `/cmd_vel` 을 0 으로 만들지만 브레이크·driver disable 은 발동하지 않았고, `STOP_COMMAND` 를 보내는 코드는 `esp32_motor_bridge` 의 서비스로만 있고 부르는 사람이 없었다. 03장에 **「중계 구현」 절 신설**: 중계는 `esp32_motor_bridge_node` 가 `/proximity/protective_stop` 을 **직접 구독**해 수행하며 `safety_gate` 를 거치지 않는다(지연 — 홉을 늘리지 않는다, 독립성 — 게이트가 죽어도 보호정지는 전달돼야 한다). 두 경로는 중복이 아니라 서로 다른 것을 막는다는 표를 함께 적었다. **상승 엣지 3회 반복**은 03장 메시지 표의 "즉시, 3회 반복 전송" 을 따른 것이고(정지는 ACK 를 기다리지 않는 로컬 실행이라 재전송이 유실의 유일한 보장), 매 프레임 중계하지 않는 이유는 `PROXIMITY_STATE` 10~20Hz 가 50Hz `DRIVE_COMMAND` 와 시리얼을 다투기 때문이다. 눌린 동안의 200ms 재확인은 유실 대비가 아니라 **눌린 채로 ESP32 가 재부팅하는 경우**를 위한 것임을 명시했다(상승 엣지는 이미 지나가 다시 오지 않는다). TBD-HW-011 에 중계 위치를 확정으로 기록하고 **남은 값이 왕복 지연 실측 하나**임을 적었다(CTRL-18, 실차 필요. 300ms 초과 시 ESP32 간 UART 대체안). CTRL-18·06장 FR-011 을 "중계 구현됨, 왕복 지연 미실측" 으로 갱신. 04장 24.1 에 이 경로가 체인 밖에 하나 더 있다는 것을 적었다. 변경 파일: `README.md`, `03-제어-캘리브레이션.md`, `04-자율주행.md`, `06-테스트-보안-운영.md`, `TBD.md` |

| v2.0-r8 | 2026-08-05 | 초음파 역할 재정의 | S15P11A301-272. 센서 수리(263) 직후 임계와 탐지 능력을 **처음 실측**했고, 명세가 기대하는 「근거리 보호 정지」와 실제 능력이 달라 문서를 실측에 맞췄다. **04장 22.2 에 실측 절 신설**: `protective_stop` 발동 **0.15~0.18m**, 측정 유효 범위 0.028~2.994m, 8~10cm 고정 시 9초 연속 `True`(전환은 진입·이탈 2회, `inf` 0개 — 앞서 관측된 잦은 발동/해제는 물체를 움직인 탓이며 깜빡임이 아니다). **거리별 탐지율이 핵심이다** — 0.08~0.11m 15/15(100%), 0.43~0.65m 3~7/15(20~45%), 1.39~1.41m 1~4/15(7~27%). 1.4m 에서 여덟 번 중 일곱 번을 놓친다. **임계는 올리지 않는다**(TBD-CAL-001 확정): 0.43m 에서 이미 탐지율이 절반 미만이라 임계를 올리면 놓치는 비율이 그대로 오판이 되고, 없는 보호를 있다고 믿는 것보다 좁은 보호를 정확히 아는 쪽이 낫다는 판단이다. 역할을 **주 방어=LiDAR·collision_monitor(전방 0.40m) / 접촉 직전 방어=초음파(0.15~0.18m)** 로 갈라 적었다 — 임계가 로봇 반경 0.20m 안쪽이므로 이 층의 목적은 충돌 제거가 아니라 **충격 감소**다. 03장 「중계 구현」에 같은 한정을 넣고 CTRL-18 이 접촉 직전만 판정함을 명시. **없는 보호를 기대하던 서술 둘을 정정**: 24.5 합격 기준의 「사람 접근 중 안전거리보다 가까워지면 정지」와 06장 SR-009(1.5~2.0m)는 **초음파로 판정할 수 없다** — 그 거리 탐지율이 7~27%다. LiDAR·사람 탐지 기반 접근 제어의 몫으로 옮겼다. 22.2 센서 표의 초음파 주기를 15.1Hz 실측으로 갱신. 변경 파일: `README.md`, `03-제어-캘리브레이션.md`, `04-자율주행.md`, `06-테스트-보안-운영.md`, `TBD.md` |

| v2.0-r10 | 2026-08-05 | 이벤트 이미지 보존 상한 | S15P11A301-265. 25.7 에 **`events/` 보존 상한 절 신설**. run 하나가 1.2G 까지 커져 젯슨 디스크가 찼는데, 원인은 프레임 로그가 아니라(`write_frame_log` 는 이미 false 였다) **`event_cooldown_seconds`(15초)가 트랙별**이라는 것이었다 — 사람 1명당 15초이므로 6명이 상시 잡히면 24 이벤트/분이고 총량에 경계가 없다. **끄지 않는다**: 이 이미지는 31-5 봉투의 `eventImage` 가 가리키는 증빙이라 없으면 요구조자 발견 근거가 사라진다. 상한을 두고 오래된 것부터 버린다(파일명이 시간순이라 이름 정렬로 판단). **장당 크기를 실측했다 — 165KB**(1280×720, `cv2.imwrite` 기본 화질, 실물 프레임 12장 중앙값, 최소 163·최대 168KB). 따라서 `events_max_bytes: 500MB` 는 약 3,100장이고 세 run 이 다 차도 1.5G 다. `events_max_files: 5000` 은 보조 안전망이며 **165KB 기준으로는 발동하지 않는다**(바이트 상한이 먼저 걸린다) — 화질·해상도를 낮춰 장당 100KB 아래가 되는 경우를 위해 남겼다. **개수가 아니라 바이트가 주 기준인 이유**는 장당 크기가 장면·overlay 에 따라 달라져 개수로 묶으면 총량이 다시 흔들리기 때문이다. **보존 시간으로 환산하지 않는다** — 사무실 2.2시간 / 시연 상황 며칠로 장면에 따라 다르고, 상한의 목적은 "며칠 치를 남긴다" 가 아니라 "장면이 어떻든 디스크가 차지 않는다" 다. 삭제는 개수·용량과 함께 로그로 남긴다(증빙 데이터이므로 조용히 지우면 안 된다). 변경 파일: `README.md`, `07-AI-탐지-음성.md` |

| v2.0-r11 | 2026-08-05 | 임무 종료 후 재시작 | S15P11A301-274. **STOP 한 번이 일회용 잠금이었다** — 관제에서 임무를 종료하면 `COMPLETED` 가 나가는 전이 없는 종단이고 `MISSION_START` 가 `SAFE_IDLE` 에서만 유효해, `mission_manager` 재기동 없이는 다시 시작할 수 없었다(실측: 「베이스캠프 복귀」 후 「탐사 시작」이 `INVALID_STATE` 거부). 시연은 반복하므로 **26.3 도식에 `COMPLETED → EXPLORING`(새 임무) 을 추가**했다. 초기화 상태를 따로 두지 않은 이유를 적었다 — 임무 종료 기록은 상태 머신이 아니라 관제 DB 의 임무 이력에 남고, `personCount` 는 encounter 파생이라 `COMPLETED` 를 붙들 이유가 없다. **`EXPLORING → COMPLETED`(관제 STOP) 화살표도 추가**했다 — `RETURNING` 이 미구현이라 종료가 복귀를 우회하는데 도식에 그 경로가 없어 `COMPLETED` 도달 길이 `RETURNING` 뿐인 것처럼 읽혔다. **낡은 기술 두 건 정정**: ① 8.2 의 `frontier_explorer` 와 06장 FR-003 이 `sentinel_exploration` 을 「미구현·소스 0개」로 적었으나 **13개 소스가 구현돼 있다**(frontier·coverage·sweep·selector·grid) — 막는 것은 Nav2 기본 꺼짐뿐이다. ② 23.4 의 종료 조건 셋 중 **구현은 운영자 종료 하나뿐**임을 명시(7분 타임아웃은 `tick()` 에 분기도 파라미터도 없다). **자동 종료를 넣지 않기로 한 결정과 근거를 기록**했다 — `frontier 소진은 지도 완성이지 수색 완료가 아니다`(`exploration_node.py`). 라이다는 360°인데 사람을 찾는 것은 전방 52° 카메라라, frontier 만 좇으면 지도는 완벽한데 구석에 쓰러진 사람은 화각에 안 들어온다(`coverage.py`). `sweep.py` 의 스텝-드웰(40° 회전 → 1.5초 응시)이 그 대책이며 **각도가 바뀌면 놓친 사람이 잡히므로 지도 완성 후에도 계속 도는 것이 맞다.** 근거를 안 적으면 다음 사람이 같은 제안을 다시 하고 같은 조사를 반복한다. 관제 버튼 「베이스캠프 복귀」는 실제로 `STOP`(임무 종료)을 보내므로 **「임무 종료」로 이름을 맞췄다**(복귀 주행 S15P11A301-246 구현 시 복귀 버튼 복원). 관제 화면 표시도 정리했다 — **상태 표시는 젯슨이 아니라 백엔드가 진실**이다(`CommandAckWriter` 가 `missions.status` 를 갱신하고 STOMP 로 밀어 준다, 원칙 27.3). 이 구분을 모르면 젯슨을 고쳐 화면을 바꾸려 하게 되고, 실제로 젯슨이 `COMPLETED` 인데 화면은 「탐사 중」인 어긋남을 겪었다. `STOP` 이 ACK 시점에 `ended_at`·`end_reason=OPERATOR_STOP`·`mission_results` 까지 넣으므로 이력은 젯슨 상태와 무관하게 보존된다. 그래서 **`COMPLETED` 표시를 「임무 완료」→「대기」**(`SAFE_IDLE` 과 동일)로, 톤도 초록→중립으로 바꿨다(대기 중인데 활동 중처럼 보였다). **「임무 종료」 버튼을 `SAFE_IDLE`·`COMPLETED` 에서 숨긴다** — 끝낼 임무가 없다. 그리고 `return` 명령의 **낙관적 표시를 `RETURNING`→`COMPLETED`** 로 고쳤다: `RETURNING` 은 미구현이라 로봇이 결코 그 상태가 되지 않는데 화면만 복귀하는 것처럼 보였고, 대기 상태에서 잘못 누르면 「복귀 중」이 떴다. 버튼 숨김과 낙관적 표시 수정이 **둘 다** 필요하다 — 버튼만 숨기면 다른 경로로 같은 명령이 나갈 때 같은 오표시가 난다. 백엔드의 `TYPE_RETURN` 은 아무도 보내지 않는 자리로 남겨 둔다(복귀 주행 구현 시 사용). 변경 파일: `README.md`, `04-자율주행.md`, `06-테스트-보안-운영.md`, `mission_state.py`, `StatusPanel.tsx`, `RobotContext.tsx` |

| v2.0-r12 | 2026-08-05 | 임무 정지 시 녹화 마감 | S15P11A301-276. 26.4 에 「임무를 멈추면 진행 중 encounter 를 끊는다」 절 신설. 종전에는 일시정지·종료가 마감 신호(`ENDED`)를 내지 않아 녹화기가 5분 `MAX_DURATION` 까지 돌았다 — 실측: `REPORTING` 중 관제 PAUSE 에 **92MB**·`endReason: MAX_DURATION`. 사람이 없는 구간까지 담겨 증빙 품질도 낮고 시연 반복 시 디스크·업로드 부담이 쌓인다. 녹화기가 임무 상태에 의존하지 않는 것은 **설계 의도**이므로(상한이 없으면 파일이 무한히 커진다) 상한은 옳고 **마감 신호를 제때 주지 않은 쪽이** 문제였다. 살려 둘 이득도 없다 — 재개가 어차피 encounter 를 버린다. 경로별로 정리했다: 일시정지·종료·**센서 실패** 는 끊고(센서 실패는 `PAUSED` 로 가는 두 번째 핸들러라 처음에 빠뜨렸다), **비상 정지는 끊지 않는다**(의도된 예외 — 원인 조사 대상이라 전후 영상이 길게 남아야 한다). 발행 순서 함정도 적었다: 노드가 발행 시점에 encounter 를 읽으므로 핸들러가 먼저 버리면 `encounterId` 가 빠져 녹화기가 무시한다. 그래서 `Transition` 이 대상 encounter 를 **들고 가게** 바꿨다 — 핸들러가 발행 순서를 신경 쓰지 않고 정리할 수 있다. 실기동: 후보를 계속 흘리는 상태로 `INTERACTING → PAUSED` 를 만들자 MP4 가 **5.5MB·약 2초 만에** 마감됐다(92MB/5분이 아니다). 변경 파일: `README.md`, `04-자율주행.md`, `mission_state.py`, `mission_manager_node.py` |

| v2.0-r13 | 2026-08-05 | 짐벌 TF 잔재 제거 | S15P11A301-279. 짐벌은 v1.1-r4 에서 프로젝트에서 제외됐고 02장 6.4 가 「미사용 확정」으로 적었으나 **`sentinel.urdf` 에 링크 3개가 남아 TF 트리에 실제로 발행되고 있었다.** 더구나 **라이다와 카메라가 그 체인에 매달려** 있어 `base_link → gimbal_base_link → gimbal_roll_link → gimbal_pitch_link → lidar_link` 였다 — 명세 35-2 권장 트리는 `base_link` 직결이다. 세 짐벌 조인트가 **모두 항등**(xyz `0 0 0`, rpy `0 0 0`)이어서 재부모화하며 origin 을 그대로 두면 기하가 보존된다. 실측으로 확인했다: `base_link → lidar_link` `[0,0,0.020]`, `→ camera_link` `[0,0,0]`, `→ camera_optical_frame` RPY `[-90,0,-90]` 이 **정리 전후 동일**하고 `gimbal_*` 프레임은 사라졌으며 SLAM 도 정상이다(`/map` 0.50Hz, `/scan` 11.61Hz). 정리한 이유는 SLAM 이 `lidar_link` 로 스캔을 base 에 옮기므로 **하중을 받는 체인에 쓰이지 않는 링크가 끼어 있으면 나중에 누가 그 조인트를 만졌을 때 지도가 조용히 틀어진다**는 것이다. 문서는 폐기라고 적고 코드는 발행하는 불일치였다 — v2.0 원칙의 반대 방향이다. **`lidar_link`·`camera_link` 오프셋은 여전히 미실측**임을 35-2 에 명시했다(라이다 z=0.02 외 전부 0, IMU 도 같은 상태 TBD-HW-012). 이 정리는 값을 바꾸지 않았다. 변경 파일: `README.md`, `03-제어-캘리브레이션.md`, `sentinel.urdf` |

| v2.0-r14 | 2026-08-05 | AI 탐지·음성 문서 분리 | S15P11A301-287. 기존 `07-AI-탐지-음성.md`, `docs/ai/detection/*`, `ai/voice/docs/*`에 분산된 설계·실측·실행 기록을 `07-AI-탐지.md`와 `08-AI-음성.md`로 통합했다. 음성 현재 운영안을 **Jetson Silero VAD → L40S FastAPI Qwen3-ASR-1.7B → GMS gpt-5.4-mini → 규칙 위험도 → 승인 사전 녹음 WAV**로 정정하고, 로컬 Whisper·로컬 LLM·동적 TTS·STT 앞 DeepFilterNet은 미채택 이력으로 분리했다. 변경 파일: `README.md`, `07-AI-탐지.md`, `08-AI-음성.md`, AI 코드·문서 링크 |

| v2.0-r15 | 2026-08-06 | **구동계 전륜 서보 조향 전환** | 하드웨어 변경 반영. 프레임 앞부분의 **수동 캐스터 바퀴 2개를 제거하고 유아전동차 기존 조향 앞바퀴 2개를 복구**했으며, 타이로드에 **DS51150-12V 서보모터**를 직결해 조향한다(제어는 모터 ESP32). 전진·후진은 후륜 좌·우 RS540 2개 + BTS7960 2개를 그대로 쓴다. **즉 v1.1-r5(2026-08-01) 의 차동 구동 전환을 되돌려 구동과 조향을 다시 분리**했고, 운동학이 전륜 조향(자전거·Ackermann 근사)으로 돌아갔다. **가장 큰 파급은 제자리 회전 불가다** — `v≈0`·`ω≠0` 명령을 실행할 수 없어 `vehicle_kinematics` 가 이를 거부하고 진단으로 보고하며(03장 34-2), Nav2 의 rotation shim·`spin` 복구·RPP `use_rotate_to_heading` 를 모두 끄고 `minimum_turning_radius` 를 넣어야 한다(04장 24.1). planner 는 NavFn 유지 vs Smac Hybrid-A\* 전환이 미결이며 **판단 기준은 `R_min` 실측 하나**다. **프로토콜은 와이어 포맷이 바뀌지 않는다** — 2026-08-01 에 호환용 예약으로 남겨 둔 `target_steering_mdeg`·`max_steering_rate_mdps`·`steering_actuator_cmd`·`measured_steering_mdeg` 자리를 되살려 쓰며, 종전 03장 payload 표가 그 예약 필드를 빠뜨려 코드(14/15/16 bytes)와 어긋나 있던 것도 함께 정정했다. **조향은 개루프다** — 서보가 내부 폐루프로 각도를 유지하고 외부로 출력하지 않아 MT6701 은 후륜 2개를 유지하고, **링키지 이탈·서보 고장을 전기적으로 감지할 수 없다.** 감지 수단은 IMU 기대 yaw rate 대조 하나이며(`STEERING_RESPONSE_MISMATCH`) 그것도 선회를 시도할 때만 드러난다(03장 34-9, SR-013). **오도메트리는 결론이 그대로이고 근거가 강해졌다** — 캐스터 슬립이 없어진 대신 후륜 스크럽과 개루프 조향 때문에 엔코더로 yaw 를 만들 근거가 **아예 없어져**, EKF 의 「엔코더 `vx` 만·IMU `vyaw` 만」 입력 선택이 선택이 아니라 유일한 구성이 됐다(04장 23.2). **전원·안전**: 서보는 12V 모터 계통의 **E-Stop 하류**에서 급전해 차단점을 하나로 유지하고(저전압 DC-DC 레일에 올리지 않는다 — stall 전류), 그 대가로 **E-Stop 후 조향각은 정의되지 않는다**(서보 무여자). 소프트웨어 정지에서는 반대로 **조향각을 마지막 값으로 유지**한다 — 급정지 순간에도 관성 주행이 남으므로 중립으로 꺾으면 의도한 궤적을 벗어난다(03장 34-7, SR-014). 예외는 부팅·리셋 직후와 정차 후 `SAFE_IDLE` 복귀 둘뿐이다. **폐기했던 TBD-HW-008 을 「전륜 서보 조향 기하」로 부활**시켰고(휠베이스 `L`·`δ_max`·`R_min`·서보 중립/엔드포인트 µs·히스테리시스·신호 규격·stall 전류, **즉시**), TBD-CAL-002 의 트랙폭 `W` 는 필수 파라미터에서 스크럽 정량화·전자 차동 검토용으로 역할이 바뀌었다(전자 차동은 **기본 미적용** — 초기에는 좌·우 동일 속도, 실측 후 필요하면 추가). 시험은 CTRL-15 를 「거부 확인」으로 바꾸고 CTRL-24~27(서보 스윕·슬루레이트·단절 시 각도 유지·링키지 이탈 감지)을 신설했으며, CAL-04 를 조향 기하 항목으로 교체했다. **소프트웨어는 아직 차동 구동을 가정한다** — 04장 8.2 에 「전륜 조향 전환이 남긴 소프트웨어 갭」 절을 신설해 `vehicle_kinematics`·`esp32_motor_bridge`·모터 펌웨어·`esp32_sensor_bridge` 정운동학·`nav2.yaml` 다섯 곳을 미구현으로 명시했고, **지금 상태로 안전 체인을 켜면 `ω` 가 차동 역운동학을 타고 후륜을 반대로 돌려 앞바퀴가 고정된 채 노면을 문지른다**는 것도 적었다(`enable_safety`·`enable_nav2` 기본 false 가 이 사고도 막고 있다). 03장 35-2 의 TF 트리 링크 이름을 `sentinel.urdf` 실제 값(`rear_drive_wheel_link`·`front_*_wheel_link`)으로 정정했고 — 「URDF 가 이 트리와 일치한다」던 문장이 이 부분은 사실이 아니었다 — **조향각을 TF 에 반영하지 않는 이유**(개루프라 실제 각도가 없고, 목표각을 실제각처럼 발행하면 없는 측정을 만든다)를 함께 적었다. 변경 파일: `README.md`, `01`, `02`, `03`, `04`, `05`, `06`, `TBD.md` |

| v2.0-r16 | 2026-08-06 | 스택 진입점 정리 | S15P11A301-294. **`demo_down.sh` 가 systemd 서비스를 모르는 채로 프로세스만 죽여, 내려도 5초 뒤 되살아났다** — 실측 journalctl: `09:20:24` demo_down 실행 → `09:20:28` `Main process exited, code=exited, status=1/FAILURE` → `09:20:33` `Scheduled restart job, restart counter is at 1`. launch 는 SIGINT 에 exit 1 로 끝나므로 `Restart=on-failure` 가 그것을 **고장으로 읽는다.** 즉 서비스가 active 인 동안 프로세스를 죽이는 경로는 **항상 무효**였고, 사용자는 「내렸는데 스트리밍·라이다가 계속 된다」로 겪었다. 이제 유닛 상태를 보고 `systemctl stop` 을 탄다(cgroup 정리라 패턴 목록보다 촘촘하다). 프로세스 정리 경로는 남긴다 — 서비스를 끈 개발 구성에서 손으로 띄운 스택을 같은 명령으로 내려야 한다. sudo 를 못 쓰면 **헛된 kill 을 하지 않고 exit 1** 로 끝낸다(죽여 놓고 성공했다고 말하지 않는다). `demo_up.sh` 는 서비스가 돌고 있으면 **이중 기동을 거부**한다(카메라 단일 오픈 32-3, MediaMTX 중복 바인딩 — 증상이 「안 뜬다」가 아니라 영상 간헐 끊김이라 찾기 어렵다). 또 **기동 시 브랜치·커밋·dirty 를 출력**한다: 이 워크스페이스는 symlink 설치라 체크아웃된 코드가 곧 실행 코드이고 서비스가 enabled 인 동안 부팅만으로 스택이 뜨므로, 기능 브랜치를 둔 채 재부팅하면 로봇이 **경고 없이 그 코드로** 뜬다(08-05 저녁에 실제로 그 상태였다). 막지 않고 journalctl 에 남기는 이유는 브랜치 실기동 검증이 정당한 사용이기 때문이다. **`start_sentinel.sh`·`stop_sentinel.sh` 제거** — 17개 중 5개만 덮어 탐지(1795MB)·SLAM 이 남았고 그것이 S15P11A301-192 메모리 오진을 **두 번** 낸 경로다. 내리는 명령이 둘이면 한쪽은 반드시 낡는다. **`viz_up.sh`·`viz_down.sh` 는 남긴다** — S15P11A301-216 이 이 쌍을 8765 무인증 노출의 완화 수단으로 설계하고 `viz_address` 기본값 뒤집기를 의도적으로 기각했다. 지우면 06장·04장 8.5 가 인용하는 완화가 사라져 「없는 보호를 있다고 믿는」 상태가 된다. 다만 그 완화가 `enable_viz` 기본값 true + 서비스 enabled 로 **이미 무력화**된 것을 실측했다(8765 가 공인 IP 에 상시 개방) — S15P11A301-295 로 분리. 프로세스 표를 15→**17개**로 갱신(`cloud_bridge`·`foxglove_bridge` 가 기본으로 뜬다). `start_sentinel.sh` 가 갖고 있던 `.env.local` IP 대조 검사가 함께 사라진 것도 명시했다(로컬 dev 서버 한정 위험, 수동 확인 명령 기록). 변경 파일: `README.md`, `01-프로젝트-개요.md`, `06-테스트-보안-운영.md`, `TBD.md`, `scripts/README.md`, `demo_up.sh`, `demo_down.sh`, `ros_env.sh`, `viz_down.sh`, `ci/validate_repository.sh`, `sentinel_mission/README.md` |

| v2.0-r17 | 2026-08-06 | 8765 노출 완화 서술 정정 | S15P11A301-295. 06장 위험 표와 04장 8.5 가 젯슨 8765(`foxglove_bridge`, 읽기 무인증) 노출의 완화를 **「볼 때만 켜고 보고 나면 `viz_down.sh` 로 끈다」** 로 적고 있었다. **그 완화는 적용할 수 없다** — `LiveMap.tsx:44` 가 `wss://jetson.sentinel-ugv.xyz:8765` 로 붙어 **관제 웹의 실시간 지도가 이 브릿지에서 `/map`·`/pose` 를 직접 받는다**(8.6). 즉 끄는 것은 노출을 줄이는 행동이 아니라 **관제 지도를 끄는 행동**이다. 실측: `ss -tlnp` 로 8765 가 `0.0.0.0` 에 열려 있고 부모가 `demo.launch.py`(sentinel-demo.service)다 — 사람이 `viz_up.sh` 를 부른 것이 아니라 `enable_viz` 기본값 `true` + 서비스 enabled 로 **부팅만으로 열린다.** **완화가 사라진 것이 아니라 처음부터 다른 것이었다** — S15P11A301-216 은 브릿지를 개발 도구로 보고 그 완화를 설계했고(그때는 성립했다), 224·227 이 브릿지를 관제 웹 부품으로 만든 시점에 구조적으로 무효가 됐다. 실제로 범위를 좁히고 있는 것은 **쓰기 차단(`capabilities: [connectionGraph]`)·토픽 6개 화이트리스트·TLS** 이고 카메라 영상은 제외돼 있다. 유출 범위는 지도·로봇 위치·스캔·TF 이며 개인정보나 조작 권한이 아니다. **없는 것은 인증 하나**다(36-4 가 MVP 범위 외로 결정). 그래서 이번 변경은 코드가 아니라 서술을 고친다 — 위험 표의 완화 칸을 「완화 없음, 범위만 좁힘」으로, 8.5 를 「노출 구간은 스택이 떠 있는 동안 전부」로 바꿨다. `viz_up.sh`·`viz_down.sh` 의 역할도 **「노출을 줄이는 수단」에서 「스택을 재시작하지 않고 브릿지만 붙이거나 떼는 수단」** 으로 다시 적었다(재시작은 메모리에만 있는 SLAM 지도를 잃는다, S15P11A301-171). `--local` 안내에는 **그 모드에서 관제 지도가 동작하지 않는다**는 것을 명시했다 — 노출을 줄이려고 시연 중에 쓰면 지도를 잃는다. `demo.launch.py` 는 요약 주석이 「기본 꺼짐」이라고 적어 실제 기본값(`true`)과 어긋났으므로 코드에 맞췄고, 기본값 선언 자리에 **「노출을 줄이려고 false 로 되돌리지 말 것」** 을 근거와 함께 남겼다. **왜 지금 적는가**: 문서가 완화했다고 적으면 다음 사람은 노출을 다시 점검하지 않고, 더 나쁘게는 그 완화를 적용하려다 관제 지도를 끈다 — v2.0 의 36-4 와 같은 형태의 위험이다. 인증(토큰·리버스 프록시·IP 허용)은 관제 클라이언트 수정이 따라오므로 시연 이후로 남겼다. 변경 파일: `README.md`, `04-자율주행.md`, `06-테스트-보안-운영.md`, `scripts/README.md`, `demo.launch.py` |

| v2.0-r18 | 2026-08-06 | 관제 화면 글꼴 확정 | S15P11A301-302. 28장에 **28.7 「글꼴」 절 신설**. 본문을 Pretendard → **Asta Sans**(42dot, OFL 1.1), 고정폭을 JetBrains Mono → **D2Coding**(NAVER, OFL 1.1)으로 바꾸고 CDN `@import` 두 줄을 자체 호스팅(`frontend/public/fonts/`)으로 옮겼다. **핵심은 D2Coding 에서 한글을 뺀 것이다** — 서브셋에 `U+AC00-D7A3` 이 없으므로 `font-mono` 스팬의 한글이 스택 다음 자리의 Asta 로 폴백하고, 「데이터 스트립만 고정폭」이 개발자 규율이 아니라 **폰트 스택 자체로 강제**된다. 이 UI 는 `font-mono` 를 78곳에서 쓰고 그중 16곳이 순수 한글 문장이라 손으로 지키는 방식은 유지되지 않는다. `scripts/build-fonts.sh` 의 `D2_RANGES` 에 한글을 추가하면 이 보장이 조용히 깨지므로 스크립트·CSS·문서 세 곳에 경고를 남겼다. **종전 상태가 이미 어긋나 있었다**: `--font-mono` 가 정의조차 안 되어 JetBrains Mono 를 5굵기 받아 놓고 한 번도 쓰지 않았고, `font-mono` 는 Tailwind 기본 스택으로 떨어졌다 — 그 스택에 한글이 없어 한글 16곳이 OS 폰트로 갈렸다(Windows·macOS 불일치). **폰트 파일 cmap 실측**: Asta 는 한글 11,172자 전부를 담고 있고(한자·조합형 자모 `U+1100-11FF` 는 없다) `tnum` 이 실재한다(기본 숫자 폭 488~610/1000 → tnum 593). **가변 채택 근거**: 이 UI 는 굵기 4개를 쓰므로 정적이면 384KB × 4 ≈ 1.5MB, 가변은 하나로 300~800 전부를 덮어 914KB 다. 실측 woff2 합계 **996.5KB**(Asta 한글 913.6 / 라틴 53.5 / D2Coding 14.4 · Bold 15.0) — 종전 Pretendard 는 `unicode-range` 없이 9굵기를 통짜로 선언해 굵기당 748KB 였다. **자체 호스팅 근거**: CSS `@import` 는 렌더를 막고 이 GCS 는 외부망 없는 로컬 시연에서 돈다. 한글 서브셋은 `preload` 하지 않는다(914KB 가 첫 페인트 대역을 먹는다) — 라틴과 D2Coding 만 preload 하고 한글은 `swap` 에 맡긴다. `tabular-nums` 는 **sans 문맥의 수치에만** 붙였다(임무 목록 한 줄). 고정폭 스팬은 이미 자릿수가 맞아 중복이며, 임무 요약 스트립이 그 경우다. **미확인**: `text-[9px]`·`text-[10px]` 한글에서 Asta 가 읽히는지는 실기 확인이 필요하다(프런트 담당). 변경 파일: `README.md`, `05-통신-서버-영상.md`, `frontend/app/globals.css`, `frontend/app/layout.tsx`, `frontend/app/blackbox/page.tsx`, `frontend/app/blackbox-experiment/page.tsx`, `frontend/scripts/build-fonts.sh`(신설), `frontend/public/fonts/*`(신설), `frontend/ATTRIBUTIONS.md` |

| v2.0-r19 | 2026-08-06 | 부팅 자동 기동을 시각 동기 뒤로 | S15P11A301-306. 06장에 **37-3-1 절 신설**. **부팅으로 뜬 스택만 SLAM 지도가 안 나오고 영상이 끊기며 ESP32 핸드셰이크가 폭주하는데, 손으로 `demo_down.sh` → `demo_up.sh` 하면 멀쩡했다.** 원인은 스크립트가 아니라 **시계**다 — 이 젯슨은 RTC 백업 배터리가 없어 부팅마다 시계가 **두 번** 점프하고, 유닛이 그 사이에 뜬다. 실측 저널: `1970-01-01 09:00:41` 커널 부팅(시계 = epoch) → timesyncd 가 **마지막 기록 시각으로 복원**(1차 점프 56년, NTP 아님) → `13:57:24` 유닛 기동 → `14:01:12` `Initial synchronization to 192.168.0.1`(**2차 점프 +3분 22초**, 스택이 이미 4분째 돌던 중). 직전 부팅도 동일(`13:44:59` 기동 → `13:49:17` 에 +3분 52초) — **재현된다.** ROS 2 는 시스템 시계로 스탬프를 찍으므로 2차 점프가 그대로 파손이 되는데, **증상이 전부 다른 곳에서 나타나 시계를 의심하기 어렵다**: `slam_toolbox` 는 TF 캐시가 미래 시각으로 채워져 라이다 스캔을 전량 폐기하고(`the timestamp on the message is earlier than all the data in the transform cache` → 지도가 안 나온다), `stream_pipeline` 은 `now - 마지막프레임시각` 이 점프량만큼 커져 입력 타임아웃으로 오판해 재시작하며(`입력이 232.0초간 없다`), `esp32_sensor_bridge` 는 워치독이 즉시 만료 판정되어 핸드셰이크를 **457회/7분(약 6.7Hz)** 반복한다. 조치는 유닛에 `After=/Wants=time-sync.target` 을 넣는 것인데, **그것만으로는 아무 보장이 없다** — `systemd-time-wait-sync.service` 가 disabled 면(Ubuntu 기본값) target 이 즉시 도달하고 저널의 `Reached target System Time Set` 은 「NTP 동기 완료」가 아니라 「시계에 값이 들어갔다」는 뜻일 뿐이다. 그래서 `enable` 이 함께 가야 한다. **`TimeoutStartSec` 상한이 반드시 따라온다** — 배포판 유닛이 `TimeoutStartSec=infinity`(실측 `systemctl show`)라 상한 없이 순서만 걸면 NTP 없는 시연장에서 스택이 **틀린 시계로 뜨는 것이 아니라 아예 안 뜬다**(고치려던 것보다 나쁜 실패). 90초로 잡고 근거를 drop-in 주석에 남겼다. **남는 제약**: RTC 배터리를 달면 1차 점프가 사라지고 2차도 수 초로 준다 — 이 변경은 배터리 없이 증상을 막는 쪽이므로 배터리 장착은 별건이다. 변경 파일: `README.md`, `06-테스트-보안-운영.md`, `scripts/systemd/sentinel-demo.service`, `scripts/systemd/systemd-time-wait-sync-timeout.conf`(신설) |

| v2.0-r20 | 2026-08-06 | telemetry 임무 귀속의 수명 | S15P11A301-307. 05장에 **31-5-1 절 신설**. **6초짜리 테스트 임무 하나에 궤적 9,574점·101분치가 붙어 있었다.** 임무를 STOP 해도 젯슨이 그 `missionId` 로 계속 발행한 것인데, 원인은 `mission_id` 를 **`COMPLETED` 이후에도 들고 있는 것**이다. 그 값을 남기는 것 자체는 의도였다 — `COMPLETED` 상태 메시지가 「어느 임무가 끝났는가」를 실어야 지도 저장이 임무별 디렉터리를 만든다(S15P11A301-171). 문제는 **수명**이다: `MISSION_ACTIVE_BY_STATE`(S15P11A301-190)는 상태 이름만 보므로 `PAUSED`·`ESTOP`·`EXPLORING` 이 전부 활성이고, `_pause_requested`·`_estop`·`_sensor_fault` 는 상태 제약이 없어 `COMPLETED` 에서도 받는다 — **PAUSE 한 번이면 끝난 임무의 궤적이 다시 자란다.** 실측 저널(2026-08-05): `12:17:26` START `b251f573` → `12:17:31` STOP(6초) → 새 임무 START 4회 거부(당시 `COMPLETED` 에서 시작 불가, -274 이전) → `12:38:54` **PAUSE** → `12:40:09` **RESUME** → 이후 전부 종료된 임무로 발행. 결과적으로 다음 임무 `93d6f70b` 의 **앞 78분이 비고** 그 주행이 6초짜리 임무 밑에 들어갔다. **-171 이 안전하다고 본 근거(`observe_candidates` 가 `COMPLETED` 에서 새 encounter 를 안 만든다)는 encounter 에만 해당하고 귀속에는 해당하지 않았다.** 조치는 `COMPLETED` 를 `MISSION_START` 가 아닌 신호로 떠날 때 `mission_id` 를 만료시키는 것이며, 판정을 전이 결과 한 곳에 둬서 `ESTOP`·`SENSOR_FAULT` 처럼 핸들러 표를 타지 않는 경로가 빠지지 않게 했다. 곁들여 **진행 중 임무가 있을 때 다른 임무의 `START` 를 `INVALID_STATE` 로 거부한다** — 종전에는 「이미 EXPLORING」으로 무시하면서 `reasonCode` 없이 `EXECUTED` 를 회신했고 백엔드가 그것으로 새 임무의 `started_at` 을 채워, 로봇과 관제가 서로 다른 임무를 진행 중이라고 믿었다(같은 임무의 재요청은 지금처럼 성공). **이미 쌓인 오염 데이터는 이 변경으로 고쳐지지 않는다** — 지우기 전에 임무별 행 수를 봐야 한다(테스트 임무가 실제 주행을 들고 있다). 변경 파일: `README.md`, `05-통신-서버-영상.md`, `jetson/ros2_ws/src/sentinel_mission/sentinel_mission/mission_state.py`, `.../test/test_mission_state.py` |

| v2.0-r22 | 2026-08-06 | 조각 PTS 순서 판정 기준과 동률 규칙 | S15P11A301-304. 05장 32-5 「이벤트 종료와 MP4 생성」에 판정 규칙 표를 넣었다. **사람을 발견해 녹화한 이벤트의 MP4 가 마감 단계에서 버려지고 있었다** — 실측(2026-08-06 13:23) 젯슨 `pending` 39건 중 **17건이 `RECORDING_FAILED_PTS_REGRESSION`**, 같은 임무에서 세 번 중 두 번 영상을 잃었다(서버에 등록된 진짜 발견에 증빙만 없는 상태). 원인은 리베이스가 아니다 — 같은 기간 `PTS 리베이스` 경고가 **0건**이었고, 실패 로그의 역행 sequence 가 3 간격의 규칙적 패턴이었다. 진짜 원인은 **판정에 쓰는 값**이다: `firstPts` 는 링 writer 가 조각 경계에서 읽는 「그 순간 마지막으로 밀어 넣은 입력 PTS」이지 그 조각의 첫 프레임 값이 아니고(32-5 「구현이 추가한 필드」가 33ms 오차로 적어 둔 그 값이다), 프레임을 미는 쪽(ROS 콜백 스레드)과 조각을 여는 쪽(GStreamer 스트리밍 스레드)이 달라 **이웃 조각이 같은 값을 기록**한다. 그런데 판정이 `current.first_pts <= previous.first_pts` 라 **동률이 역행으로 잡혔다.** 바꾼 것: ① 순서 판정을 `muxedPts`(= `format-location-full` 의 `first_sample` 에서 꺼낸 그 조각의 첫 샘플 PTS)로 옮기되 **모든 조각에 있을 때만** 쓴다 — 하나라도 없으면 전부 `firstPts` 로 내려간다(두 값은 기준점이 달라 1시간 오프셋이 섞이면 가짜 점프·가짜 역행이 난다), ② 동률은 통과시키고 `ptsTies` 로 보고한다(조용히 넘기면 인코더 정체를 놓친다), ③ 판정할 값이 없는 조각을 `ptsUnknown` 으로 드러낸다(종전에는 검사가 통째로 비어도 `ok: true` 와 구별되지 않았다), ④ 마감 판정 근거를 이벤트 디렉터리에 `segments.json` 으로 남긴다 — **링 버퍼가 조각 8개만 유지해 실패 몇 초 뒤에는 원본이 순환돼 사라진다**(이번 조사에서 실제로 막혔다). **검사를 없애지는 않았다** — 진짜 역행을 concat 하면 재생이 깨지므로 감소는 여전히 실패다. `firstPts` 도 그대로 둔다(3초 사전 영상 탐색에는 입력 기준이 맞다). 단위 시험 10건 추가(동률 통과 / 진짜 역행 차단 / 스레드 시차가 있어도 muxed 로 정상 판정 / 혼합 시 input 으로 하강 / 값 없음 보고). **미검증**: 실기기 회귀(사람 여러 명 장면에서 연속 3회 이벤트가 전부 MP4 로 나오는지)는 아직이다. 변경 파일: `README.md`, `05-통신-서버-영상.md`, `sentinel_recorder/segment_store.py`, `sentinel_recorder/event_finalizer.py`, `sentinel_streaming/ring_buffer.py`, `sentinel_recorder/test/test_segment_store.py`(신설) |

| v2.0-r23 | 2026-08-06 | 마감 실패 사유의 관제 전달 경로 | S15P11A301-309. 05장 32-5 에 「마감 실패는 텔레메트리로 나간다」를 넣고 telemetry 예시의 `health` 를 갱신했다. **S15P11A301-304 가 19건 쌓일 때까지 드러나지 않은 이유는 판정이 틀렸기 때문만이 아니다 — 사유가 젯슨 디스크 밖으로 나간 적이 없었다.** `mediaState` 는 `report.json` 에만 있었고 그 문자열은 백엔드 java·프런트 tsx 어디에도 없다(전체 검색 0건). 표시 누락이 아니라 **전송 부재**다: `pending_store.ready_for_upload` 가 `has_media and has_checksum` 이므로 **`event.mp4` 가 없는 실패 이벤트는 업로드가 시도조차 되지 않는다.** encounter 자체는 MQTT 로 따로 적재되므로 서버에는 미디어 없는 발견만 남고, 관제는 그것을 「아직 업로드 전」과 구별하지 못한다. 경로를 텔레메트리 `health` 로 만들었다 — `recording_manager` 의 `~/status` 가 `lastFinalizeOk`·`lastFailure` 를 싣고 `cloud_bridge` 가 `recorderOk`·`recorderLastFailure` 로 옮긴다. **성공이 사유를 지우지 않는 것이 요점이다** — 304 는 간헐 실패였고(세 번 중 두 번), 성공 한 번에 지우면 그 정상 이벤트가 직전 실패를 덮는다. 「지금 정상인가」는 `recorderOk`, 「이번 기동에 실패가 있었나」는 `recorderLastFailure` 가 답하며 재발 감지에 필요한 것은 후자다. `ENVIRONMENT_SENSOR_FAULT`(S15P11A301-258)의 자동 해제와 **반대 규칙**인데, DHT11 은 2초마다 판정되지만 마감은 이벤트가 있을 때만 판정되고 그 간격이 몇 분이라 같은 규칙을 쓰면 거의 항상 정상으로 보이기 때문이다. 부팅 복구가 찾은 `CORRUPT` 는 사유만 남기고 `recorderOk` 는 건드리지 않는다(지난 기동의 잔해이지 이번 기동의 마감 결과가 아니다). 판정 로직을 `recording_health.py` 로 분리한 이유는 `recording_manager_node` 가 `rclpy` 를 import 해 CI 에서 돌지 않기 때문이다 — 여기서 틀리면 화면이 조용히 거짓말을 하므로 시험이 붙는 자리에 뒀다. 단위 시험 14건 추가(마감 상태 7건, 계약 7건). **미구현**: 백엔드 적재와 화면 표시. `health` 두 필드를 저장·노출하는 것은 S15P11A301-309 에서 백엔드(이원빈)·프런트로 위임돼 있다. 변경 파일: `README.md`, `05-통신-서버-영상.md`, `common/schemas/telemetry.schema.json`, `sentinel_recorder/recording_health.py`(신설), `sentinel_recorder/recording_manager_node.py`, `sentinel_recorder/README.md`, `sentinel_bridge/message_mapper.py`, `sentinel_bridge/cloud_bridge_node.py`, `sentinel_recorder/test/test_recording_health.py`(신설), `sentinel_bridge/test/test_message_contract.py` |

| v2.0-r24 | 2026-08-06 | 임무 상태 표시의 출처를 로봇 보고로 | S15P11A301-316. 04장 26.3 「관제 화면의 상태 표시는 백엔드가 진실이다」를 고쳐 **두 writer 의 역할 분담**을 적었다. **차체가 멈춰 있는데 화면이 「탐사 중」인 시간이 대부분이었다** — 새로고침하거나 모드 토글을 왔다갔다 하면 「탐사 중」으로 되돌아갔다. 원인은 `missions.status` 가 사실상 **「마지막으로 수락된 명령」의 원장**이었던 것이다(`RobotStateWriter` 가 S15P11A301-298 에서 **수동 여부만** 옮기기로 좁혀 뒀다). 그래서 `START`·`RESUME` 한 번에 `EXPLORING` 이 고착됐다. 실측 3갈래: ① 모드 토글이 `MOTOR_BOARD_NO_ACK` 로 거부되면 `CommandAckWriter` 가 early return 해 아무것도 쓰지 않고 푸시도 없다 — 낙관적 「수동 조종」이 최대 15초 뒤 백업 폴링에서 「탐사 중」으로 되돌아갔고 조작자에게는 가만히 있는데 상태가 바뀌는 것으로 보였다(운영 API 명령 이력에서 MANUAL·AUTO 전부 거부, `RESUME` 이 `INVALID_STATE` 로 2건 — 화면만 `PAUSED` 라고 믿은 결과다), ② 로봇이 스스로 `EXPLORING` 을 벗어난 것(encounter 진행·센서 실패 `PAUSED`·`ESTOP`·`ERROR`·`mission_manager` 재시작)은 어디에도 안 적혔다, ③ `EXPLORING` 으로 열린 임무는 `STOP` 없이 닫히지 않아 다음 페이지 로드가 그것을 이어받았다(로봇이 꺼져 있어도). 고친 것: `RobotStateWriter` 가 1Hz `ROBOT_STATE` 의 `missionState` 를 `status` 에 반영하고, **라이프사이클**(`started_at`·`ended_at`·`end_reason`·`mission_results`)은 계속 `CommandAckWriter` 만 건드린다 — 두 writer 가 같은 칸을 두고 싸우지 않게 하는 경계가 이것이다. 어긋나면 **로봇의 보고가 이긴다.** ACK 와 상태 메시지의 순서는 젯슨 단일 MQTT 연결과 서버 Paho 콜백 한 스레드가 보존하므로 `START` ACK 뒤에 옛 `SAFE_IDLE` 이 도착하는 경합은 없다. **모르는 상태값은 쓰지 않고 경고만 남긴다** — 26.2 의 12개를 백엔드·프런트 양쪽에서 전수로 적는다(모르는 값을 흘리면 프런트 라벨 표에서 조용히 빠지고 화면이 옛 상태를 계속 보여준다. 그것이 고치려는 증상 자체다). 젯슨이 임무 밖 상태(`SAFE_IDLE`·`COMPLETED`)에서 `activeMissionId` 를 비우므로(`MISSION_ACTIVE_BY_STATE`) 그때는 **로봇 이름으로 열린 임무를 찾아** 쓴다 — 그것이 없으면 「로봇은 재시작해 대기인데 DB 는 `EXPLORING`」을 고칠 수 없다. 프런트는 상태 표를 `missionStatus.ts` 로 분리해 시험이 지키게 했고(`motionFromLatest` 와 같은 이유), 거부 ACK 를 확인한 순간 서버 상태를 즉시 다시 반영한다(종전에는 사유 토스트만 띄우고 표시는 최대 15초 거짓말을 유지했다). 새로고침 복구도 같은 경로를 타게 해 `controlMode` 가 함께 복원된다 — 종전에는 「수동 조종」인데 토글은 「자율」인 화면이 최대 15초 나왔다. 시험 14건(백엔드 9, 프런트 5). **범위 밖**: 모드 토글이 `MOTOR_BOARD_NO_ACK` 로 전부 거부되는 것은 별개 원인이며 별도 티켓이다. 변경 파일: `README.md`, `04-자율주행.md`, `RobotStateWriter.java`, `RobotStateData.java`, `RobotStateWriterTest.java`, `missionStatus.ts`(신설), `RobotContext.tsx`, `tests/missionStatus.test.ts`(신설) |
| v2.0-r25 | 2026-08-07 | 영상 없는 발견의 사유 표시 | S15P11A301-311. 05장 32-5 에 「화면은 「영상 없음」을 세 가지로 나눠 말한다」를 넣고 28.6 에 그 원칙을 걸었다. **종전 화면은 「영원히 영상이 없는 발견」과 「아직 업로드 전」을 같은 문구로 보여 줬다** — 앞은 젯슨을 열어야 하고 뒤는 기다리면 되는데 대응이 갈리는 구별이 화면에 없었다. S15P11A301-304 가 19건 쌓일 때까지 드러나지 않은 마지막 고리다(원인은 304, 전달 경로는 309, 적재는 310). 실측(운영 API, 2026-08-06 11:57~12:07 임무 셋): **발견 10건이 전부 미디어 0건이고 텔레메트리는 전 구간 `recorderOk: false`·`RECORDING_FAILED_NO_SEGMENTS`** 인데 화면은 10건 모두 「연결된 영상이 없습니다」였다. **판정 근거가 encounter 가 아니라 임무 시계열이라는 것이 이 표시의 전부다** — 실패한 이벤트는 `event.mp4` 가 없어 업로드 경로를 타지 않으므로 사유가 미디어 행에 실릴 길이 없고, telemetry `health` 로만 온다. 그래서 발견 시각과 텔레메트리 점을 맞춰 귀속시킨다: **마감 창(발견 종료 뒤 120초, 다음 발견이 그 안에 시작하면 거기서 끊음)의 마지막 `recorderOk` 판정**이 실패/업로드 전을 가르고, 판정값이 없으면 「정상」이 아니라 「판정 근거 없음」이다. **판정은 사유가 아니라 `recorderOk` 가 한다** — 래치는 성공에 지워지지 않으므로 사유 유무로 판정하면 한 번 실패한 기동의 모든 발견이 실패로 보이고, 반대로 사유 **변화**로 판정하면 같은 사유가 반복될 때(PTS 동률은 실제로 반복됐다) 두 번째 실패를 놓친다. 창의 **마지막** 점을 보는 이유는 마감이 POST_RECORDING 3초 뒤에 시작해 창 앞쪽에는 아직 직전 마감의 결과가 실려 있기 때문이다. 원문 `mediaState` 는 한국어 문구와 **함께** 보여 준다(툴팁만 두면 마우스 없는 화면에서 열리지 않고, 젯슨 `report.json` 과 눈으로 대조해야 할 때가 있다). 모르는 사유는 원문 그대로 — 젯슨이 `RECORDING_FAILED_{사유}` 로 만들어 값이 늘어난다. 임무 요약에 「녹화기」 칸을 더해 발견을 열지 않아도 실패 기록이 보이게 했다. 시험 17건 신설. **한계**: ① 래치가 임무 경계에서 지워지지 않아 요약 칸은 「이 임무가 실패했다」가 아니라 「실패 기록이 남아 있다」이며 시점은 발견별 판정이 좁힌다, ② `EncounterSummary` 에 미디어가 없어 세 갈래는 발견을 **열어야** 보인다(목록 표시는 미구현 — 서버가 요약에 미디어 유무를 실어야 한다). 변경 파일: `README.md`, `05-통신-서버-영상.md`, `frontend/features/telemetry/recorderStatus.ts`(신설), `frontend/app/blackbox/page.tsx`, `frontend/lib/api.ts`, `frontend/app/blackbox-experiment/mockApi.ts`, `frontend/tests/recorderStatus.test.ts`(신설), `frontend/tests/motionReading.test.ts` |

> 문서를 변경할 때는 위 표에 한 줄을 추가하고, 어느 파일이 바뀌었는지 함께 적는다. 파일별 버전은 두지 않는다.

## 결정 상태 표기
| **표기**  | **의미**                    | **처리 원칙**                                   |
|-----------|-----------------------------|-------------------------------------------------|
| 확정      | 팀이 해당 방식으로 구현     | 변경 시 팀 합의 및 변경 이력 기록               |
| 잠정 확정 | 추천안을 우선 적용          | 실험 결과에 따라 대체안으로 전환 가능           |
| TBD       | 부품 확인·측정·검증 후 결정 | 담당자와 확정 기한을 반드시 지정                |
| 확장      | MVP 완료 후 추가            | 필수 기능 일정에 영향을 주지 않는 범위에서 수행 |

## 검수 기준

본 문서는 **2026-07-27까지 프로젝트 대화에서 확정되거나 실제 장비로 확인된 최신 결정**을 유일한 설계 기준으로 사용한다. 서로 다른 시점의 결정이 충돌하면 더 나중에 사용자가 직접 확정한 내용을 우선한다. 요약 장(1~20장)과 상세 장(21~38장)의 기술이 어긋나면 **상세 장을 규범으로 우선**한다.

- 차량은 **BMW M7 유아전동차의 구동 베이스를 활용**한다.
- 고수준 컴퓨팅은 Jetson Orin Nano 8GB, 저수준 주행 제어는 ESP32 WROOM-32 2개(모터 제어용·센서 제어용 분리)가 담당하며 Raspberry Pi 5는 차량에 탑재하지 않는다.
- Jetson↔ESP32 제어 링크는 각 ESP32마다 독립된 유선 USB Serial을 사용한다. ESP32 Wi-Fi/Bluetooth는 모터 제어·E-Stop·watchdog 경로에 사용하지 않는다.
- 실제 Jetson에서 확인한 `JetPack 6.2.1+b38`과 ROS 2 Humble을 소프트웨어 기준선으로 고정한다.
- **구동계는 후륜 좌·우 RS540 FD-12V RPM14000 모터 2개가 전진·후진을, 전륜 타이로드에 직결된 DS51150-12V 서보모터 1개가 조향을 담당한다**(2026-08-06 전환 — 2026-08-01 차동 구동 전환을 되돌렸다). 조향 기하가 있어 최소 회전반경 제약이 있고 **제자리 회전은 할 수 없다.** 조향은 서보 내부 폐루프이므로 외부 각도 피드백 없이 개루프로 운용한다. 모터 드라이버(BTS7960 2개), 엔코더(MT6701 2개 — 후륜만), 초음파(HC-SR04), 온습도(DHT-11), 배터리(모터용 유아전동차 내장 12V·Jetson용 대용량 보조배터리 분리 급전)는 모델·수량을 확정했고, 조향 서보는 모터 계통 12V 의 **E-Stop 하류**에서 급전한다. 차체 IMU는 센서 ESP32의 I2C/SPI에 연결하는 구조와 수량 1개를 확정했고 모델·핀·장착 오프셋은 TBD-HW-012로 관리한다. 감속비·정지 전류·**휠베이스 `L`·최대 조향각 `δ_max`·최소 회전반경 `R_min`·서보 중립/엔드포인트**(TBD-HW-008) 등 세부 실측값과 전력 예산은 계속 TBD로 관리한다(부록 H).
- 사람 탐지는 사전학습 YOLO26n Detect를 사용한다. 파인튜닝은 교차 평가에서 일반화 성능이 하락해 미채택했으며, Pose는 사람 감지 시에만 실행하는 조건부 파이프라인으로 운영한다.
- 짐벌은 프로젝트에서 완전히 제외한다. 카메라·LiDAR는 차체에 고정 마운트하며 정적(static) TF만 사용한다. 기존 FR-024(짐벌 NAV_LOCK/CENTER_LOCK)는 폐기한다.
- 피해자 음성 출력은 블루투스 스피커, 입력은 BRIO 100 내장 마이크를 사용한다. VAD는 Jetson, STT는 L40S FastAPI Qwen3-ASR-1.7B, 정보 추출은 GMS `gpt-5.4-mini`, 안내는 승인된 사전 녹음 WAV로 운영한다. 동적 TTS와 Jetson 로컬 STT·LLM은 사용하지 않는다.
- 통신 계약(REST·MQTT·WebSocket 메시지)은 31장을 유일 기준으로 하며 12장은 요약이다.

# 문서 구성

역할별 파일과 TBD 대장으로 분리한다. 장 번호는 전역 연속이며, 요약 장과 규범(상세) 장은 되도록 같은 파일에 묶었다.

| 파일 | 담당 역할 | 포함 장·부록 |
|---|---|---|
| [README.md](README.md) | 전원 | 문서 규칙·버전·변경 이력·읽기 경로·용어집(부록 G)·참고 자료 |
| [01-프로젝트-개요.md](01-프로젝트-개요.md) | 처음 합류·기획 | 1~5장(개요·목표·시나리오·아키텍처), 7장(기술 스택), 15장(저장소·CI/CD), 17~20장(일정·위험·시연·완료 기준) |
| [02-하드웨어.md](02-하드웨어.md) | 임베디드·기구 | 6장(하드웨어·기구), 21장(인터페이스·전원·배선), 부록 I(BOM)·J(핀맵) |
| [03-제어-캘리브레이션.md](03-제어-캘리브레이션.md) | 임베디드·주행 | 34장(ESP32 저수준 제어·안전 통신), 35장(센서·엔코더 캘리브레이션·튜닝) |
| [04-자율주행.md](04-자율주행.md) | ROS2·주행 | 8장(자율주행 요약), 14장(상태 머신·안전 정책), 22~24장(TF·SLAM·Nav2), 26장(Mission Manager) |
| [07-AI-탐지.md](07-AI-탐지.md) | AI·탐지 | 9장(AI 요약), 25장(사람 탐지·추적·자세 판정), 30장(피해자 오케스트레이션) |
| [08-AI-음성.md](08-AI-음성.md) | AI·음성 | 33장(음성 상호작용·GPU ASR·Jetson 실행·실측) |
| [05-통신-서버-영상.md](05-통신-서버-영상.md) | 백엔드·프론트·영상 | 10~13장(영상·관제 웹·통신·DB 요약), 27~29장(Spring Boot·Next.js·DB/S3), 31장(통신 계약 규범), 32장(스트리밍·녹화), 부록 D(환경 변수) |
| [06-테스트-보안-운영.md](06-테스트-보안-운영.md) | 안전·시험·운영 | 16장(테스트 계획), 36~38장(보안·운영·요구사항 추적·인수 시험), 부록 A~C(요구사항)·E~F(실행·로그)·K(기준선)·L(파라미터) |
| [TBD.md](TBD.md) | 전원 | 부록 H — 미확정 항목 단일 대장(담당·기한 포함). 가장 자주 갱신됨 |
| [git_convention.md](git_convention.md) | 전원 | 브랜치·커밋·MR 규칙 |
| [ai/README.md](ai/README.md) | AI | AI 문서 진입점과 구현 경로 안내 |

## 전체 목차

장 번호는 전역 연속(1~38, 부록 A~L)이며, 파일 안에서는 번호가 불연속일 수 있으나 고유 식별자로 사용한다.

| 장 | 제목 | 파일 |
|----|------|------|
| 1  | 프로젝트 개요 및 배경 | [01](01-프로젝트-개요.md) |
| 2  | 목표, 범위 및 성공 기준 | [01](01-프로젝트-개요.md) |
| 3  | 최신 확정 구조와 설계 결정 | [01](01-프로젝트-개요.md) |
| 4  | 사용자·시연 시나리오 | [01](01-프로젝트-개요.md) |
| 5  | 전체 시스템 아키텍처 | [01](01-프로젝트-개요.md) |
| 6  | 하드웨어 및 기구 설계 | [02](02-하드웨어.md) |
| 7  | 소프트웨어 기술 스택 및 실행 환경 | [01](01-프로젝트-개요.md) |
| 8  | ROS2 자율주행 및 탐사 설계 | [04](04-자율주행.md) |
| 9  | AI 인식 및 센서 융합 | [07](07-AI-탐지.md) |
| 10 | 영상 스트리밍 및 이벤트 녹화 | [05](05-통신-서버-영상.md) |
| 11 | 통합 관제 웹 시스템 | [05](05-통신-서버-영상.md) |
| 12 | 통신 프로토콜 및 API | [05](05-통신-서버-영상.md) |
| 13 | 데이터베이스·시계열·S3 설계 | [05](05-통신-서버-영상.md) |
| 14 | 상태 머신 및 안전 정책 | [04](04-자율주행.md) |
| 15 | 저장소·배포·CI/CD | [01](01-프로젝트-개요.md) |
| 16 | 테스트 및 검증 계획 | [06](06-테스트-보안-운영.md) |
| 17 | 일정 및 역할 분배 | [01](01-프로젝트-개요.md) |
| 18 | 위험 관리와 대체안 | [01](01-프로젝트-개요.md) |
| 19 | 최종 시연 계획 | [01](01-프로젝트-개요.md) |
| 20 | 완료 기준 및 KPI | [01](01-프로젝트-개요.md) |
| 21 | 하드웨어 인터페이스·전원·배선 상세 설계 | [02](02-하드웨어.md) |
| 22 | 센서 수집·시간 동기화·좌표계 상세 설계 | [04](04-자율주행.md) |
| 23 | SLAM·위치 추정·미지 영역 탐사 상세 설계 | [04](04-자율주행.md) |
| 24 | Nav2 경로 계획·장애물 회피·안전 속도 설계 | [04](04-자율주행.md) |
| 25 | 사람 탐지·추적·위치 추정 상세 설계 | [07](07-AI-탐지.md) |
| 26 | Mission Manager·임무 상태 머신 상세 설계 | [04](04-자율주행.md) |
| 27 | Spring Boot 관제 백엔드·도메인·API 상세 설계 | [05](05-통신-서버-영상.md) |
| 28 | Next.js 관제 화면·조이스틱 UX 상세 설계 | [05](05-통신-서버-영상.md) |
| 29 | PostgreSQL·TimescaleDB·S3·Outbox 상세 설계 | [05](05-통신-서버-영상.md) |
| 30 | 피해자 발견·접근·상호작용 오케스트레이션 | [07](07-AI-탐지.md) |
| 31 | Jetson·Spring Boot·관제 웹 통신 설계 | [05](05-통신-서버-영상.md) |
| 32 | 영상 스트리밍·3초 링 버퍼·이벤트 녹화 설계 | [05](05-통신-서버-영상.md) |
| 33 | 피해자 음성 상호작용 설계 | [08](08-AI-음성.md) |
| 34 | ESP32 저수준 주행 제어·안전 통신 설계 | [03](03-제어-캘리브레이션.md) |
| 35 | 센서·엔코더 캘리브레이션 및 튜닝 | [03](03-제어-캘리브레이션.md) |
| 36 | 보안·개인정보·데이터 보호 정책 | [06](06-테스트-보안-운영.md) |
| 37 | 운영·모니터링·장애 복구 설계 | [06](06-테스트-보안-운영.md) |
| 38 | 요구사항 추적표·최종 인수 시험 | [06](06-테스트-보안-운영.md) |
| 부록 A | 기능 요구사항 상세 | [06](06-테스트-보안-운영.md) |
| 부록 B | 비기능·안전 요구사항 | [06](06-테스트-보안-운영.md) |
| 부록 C | 주요 ROS 토픽·액션 | [06](06-테스트-보안-운영.md) |
| 부록 D | 환경 변수 예시 | [05](05-통신-서버-영상.md) |
| 부록 E | 초기 실행 순서 | [06](06-테스트-보안-운영.md) |
| 부록 F | 주요 로그 확인 명령 | [06](06-테스트-보안-운영.md) |
| 부록 G | 용어집 | [README](README.md#부록-g-용어집) |
| 부록 H | 최종 TBD 및 변경 관리 | [TBD](TBD.md) |
| 부록 I | 최종 BOM·전력 예산 템플릿 | [02](02-하드웨어.md) |
| 부록 J | 배선도·핀맵 확정표 | [02](02-하드웨어.md) |
| 부록 K | 소프트웨어 기준선 | [06](06-테스트-보안-운영.md) |
| 부록 L | 최종 파라미터 동결표 | [06](06-테스트-보안-운영.md) |

## 독자별 읽기 경로

**1~20장은 프로젝트 전체를 빠르게 파악하는 요약**, **21~38장은 구현 기준이 되는 상세 규범**, 부록은 요구사항·TBD 대장·기준선이다. 요약 장과 상세 장이 어긋나면 상세 장을 따른다.

| 독자 | 먼저 읽을 것 | 구현 기준 (규범) |
|---|---|---|
| 처음 합류한 팀원 | [01](01-프로젝트-개요.md)의 1~5·17장, 용어집(README) | - |
| 임베디드·기구 | [02](02-하드웨어.md)의 6장 | [02](02-하드웨어.md)의 21장, [03](03-제어-캘리브레이션.md)의 34·35장, [TBD](TBD.md), 부록 I·J |
| ROS2·자율주행 | [04](04-자율주행.md)의 8장 | [04](04-자율주행.md)의 22~24장, [03](03-제어-캘리브레이션.md)의 35장 |
| AI 탐지 | [07](07-AI-탐지.md)의 9장 | [07](07-AI-탐지.md)의 25·30장 |
| AI 음성 | [08](08-AI-음성.md)의 33.1장 | [08](08-AI-음성.md)의 33장 전체 |
| 백엔드 | [05](05-통신-서버-영상.md)의 12·13장 | [05](05-통신-서버-영상.md)의 27·29·31장 |
| 프론트엔드 | [05](05-통신-서버-영상.md)의 11장 | [05](05-통신-서버-영상.md)의 28장·31-8 |
| 영상 | [05](05-통신-서버-영상.md)의 10장 | [05](05-통신-서버-영상.md)의 32장 |
| 안전·시험 담당 | [04](04-자율주행.md)의 14장, [06](06-테스트-보안-운영.md)의 16장 | [04](04-자율주행.md)의 26장, [06](06-테스트-보안-운영.md)의 36~38장 |

# 부록 G. 용어집
| **용어**           | **설명**                                                    |
|--------------------|-------------------------------------------------------------|
| **UGV**            | Unmanned Ground Vehicle, 무인 지상 차량                     |
| **온디바이스 AI**  | 클라우드가 아닌 장치 내부에서 AI 추론을 수행하는 방식       |
| **추론**           | 학습된 모델에 새 입력을 넣어 결과를 얻는 과정               |
| **SLAM**           | 위치 추정과 지도 작성을 동시에 수행하는 기술                |
| **Nav2**           | ROS2 기반 경로 계획·주행·복구 프레임워크                    |
| **Frontier**       | 확인된 자유 공간과 미지 공간의 경계                         |
| **오도메트리**     | 바퀴·IMU 등으로 로봇 이동량을 추정한 값                     |
| **TF**             | ROS에서 좌표계 관계를 관리하는 시스템                       |
| **Costmap**        | 장애물과 이동 비용을 표현하는 격자 지도                     |
| **WebRTC**         | 브라우저 저지연 실시간 미디어 통신 기술                     |
| **WHEP**           | WebRTC 스트림 수신을 위한 HTTP 기반 표준 인터페이스         |
| **MediaMTX**       | RTSP/WebRTC 등 미디어 프로토콜 중계 서버                    |
| **TimescaleDB**    | PostgreSQL 확장 기반 시계열 데이터베이스                    |
| **MQTT**           | 차량 상태·이벤트·명령을 토픽 기반으로 교환하는 메시지 프로토콜 |
| **Encounter**      | 한 번의 사람 그룹 발견·접근·대화·보고를 묶는 도메인 단위     |
| **ByteTrack**      | 영상 프레임 간 사람별 track ID를 유지하는 다중 객체 추적기    |
| **Watchdog**       | 정해진 시간 안에 정상 신호가 없으면 안전 동작을 수행하는 감시기 |
| **S3**             | AWS 객체 스토리지                                           |
| **Presigned URL**  | 제한 시간 동안 특정 S3 객체 업로드·조회 권한을 제공하는 URL |
| **Deadman switch** | 버튼을 누르는 동안에만 장비가 동작하도록 하는 안전 입력     |
| **E-Stop**         | 비상 정지                                                   |
| **TBD**            | 추후 결정할 항목                                            |

# 참고 자료
- [NVIDIA JetPack 6.2.1 / Jetson Linux 36.4.4](https://forums.developer.nvidia.com/t/jetpack-6-2-1-jetson-linux-36-4-4-is-now-live/337335)
- [NVIDIA JetPack Archive](https://developer.nvidia.com/embedded/jetpack-archive)
- [ROS 2 Humble Ubuntu 22.04 설치](https://docs.ros.org/en/humble/Installation/Ubuntu-Install-Debs.html)
- [Ultralytics YOLO26](https://docs.ultralytics.com/models/yolo26/)
- [Nav2 공식 문서](https://docs.nav2.org/)
- [Nav2 알고리즘 선택 - 로봇 운동학별 planner·controller 지원(differential·Ackermann)](https://docs.nav2.org/setup_guides/algorithm/select_algorithm.html)
- [ROS 2 Control Humble - Wheeled Mobile Robot Kinematics (자전거·Ackermann 모델 포함)](https://control.ros.org/humble/doc/ros2_controllers/doc/mobile_robot_kinematics.html)
- [MediaMTX 공식 문서](https://mediamtx.org/docs/kickoff/introduction)
- [MQTT 5.0 OASIS 표준](https://docs.oasis-open.org/mqtt/mqtt/v5.0/mqtt-v5.0.html)
- [AWS S3 보안 모범 사례](https://docs.aws.amazon.com/AmazonS3/latest/userguide/security-best-practices.html)

본 문서는 위 명세서의 교육 목표와 제공 하드웨어를 참고하되, Sentinel UGV의 재난 탐사 시나리오와 팀의 기술 스택에 맞게 재구성한 독립 프로젝트 종합 명세서이다.
