> Sentinel UGV 통합 명세서 v2.1의 일부 — 문서 규칙·버전·변경 이력은 [README](README.md) 참조

# 부록 H. 최종 TBD 및 변경 관리 (단일 TBD 대장)

본 표는 **문서 전체에서 유일한 TBD 대장**이다. 본문에는 TBD ID 참조만 두고 별도 표를 만들지 않는다(6.7 참조). ID 체계는 구 6.7장 하드웨어 목록을 승계했다. 항목이 확정되면 확정값을 "결정 시 기록할 값" 열에 채우고, 선택 근거·측정값·결정일을 문서 변경 이력에 기록한다. 구현 중 명세와 다른 구조를 적용할 경우 실제 구현을 우선하되, 변경 이유와 장단점, 일정 영향, 대체안을 본 문서에 반영한다.

> **시연 종료 후 읽는 법:** `기한`의 주차 표기는 최초 계획을 보존한 값이다. 현재 상태는 각 행 본문의 **해소·확정·종료 시 일부 미완료** 표기를 우선하며, 끝내지 못한 항목은 후속 작업으로 본다.

| **ID**      | **미확정 항목** | **결정 시 기록할 값**                           | **담당(역할)** | **기한** |
|-------------|-----------------|-------------------------------------------------|----------------|----------|
| TBD-HW-001  | RS540 구동계 재계측 | 후륜 RS540 2개는 전진·후진만 담당하고 조향은 전륜 서보다. 실물 바퀴 지름 `0.250m`, 엔코더 기어비 82, Jetson 환산 `5.846e-07m/tick`을 반영했다. 샘플 주기 수정 뒤 `200mm/s×5초`에서 줄자 38cm·오도메트리 36.8cm로 거리 오차 3%를 확인했다. 속도별 실측은 0.20m/s 명령에서 38%, 0.25m/s에서 82%, 0.30m/s에서 99%였고, 최종 운용 대역을 0.25~0.30m/s로 고정해 저속 무릎 보정은 도입하지 않았다. 센서 펌웨어 `WHEEL_DIAMETER_MM=120`과 Jetson 거리 환산용 0.250m가 달라 보드 자체 `speed_mmps`는 참고값이다. 남은 값은 좌·우 3m 개별 스케일·정/역방향, 정지 전류·최대 속도·선회 스크럽이다 | 임베디드·기구 B | 종료 시 일부 미완료 |
| TBD-HW-002  | 모터 드라이버   | **2026-07-30 모델 확정**: BTS7960(좌 구동·우 구동·조향). **2026-08-01 차동 구동 전환으로 2개(좌 구동·우 구동)로 감축**. **2026-08-06 전륜 서보 조향 전환에도 2개를 유지한다** — 새 조향 액추에이터는 자체 드라이버를 내장한 서보이므로 BTS7960 3번째가 필요하지 않다. 채널당 연속/피크 전류 실측은 진행 | 임베디드·기구 B | 1주차 초 |
| TBD-HW-003  | 배터리·DC-DC·전원 | **2026-07-30 구성 확정**: 모터 전원(유아전동차 내장 12V 배터리)과 Jetson 전원(대용량 보조배터리)을 완전 분리(6.6·21.4). **2026-08-06 서보 레일 결정**: 조향 서보는 **12V 모터 계통의 E-Stop 하류**에서 직접 급전한다(저전압 DC-DC 레일에 올리지 않는다 — stall 전류가 커서 ESP32·센서 논리 전원이 흔들린다). 차단점을 하나로 유지해 배선·검증을 단순화하는 대가로, E-Stop 시 서보가 무여자되어 조향각이 정의되지 않는다(34-7). **2026-08-11 실물 확인**: 시연 차량은 **3계통**으로 돌았다 — 12V 모터 배터리, Jetson 전용 보조배터리, **모터 드라이버용 별도 5V 배터리**. 5V 계통은 02장 6.6 이 「2계통 확정」으로 적어 누락돼 있었고 03장 35-8 만 언급하고 있었다(정정 완료). **남은 값**: 보조배터리 정격(발표자료 기준 20V)·용량, 5V 배터리 용량, DC-DC 사양, 서보 stall 전류를 더한 피크 전력, 예상 운용 시간 | 임베디드·기구 B | 1주차 중 |
| TBD-HW-004  | 엔코더 환산 재검증 | MT6701 2개를 후륜 좌·우에 사용한다. I2C 14-bit 절대각(16384 counts/rev), 주소 `0x06`/`0x46`, GPIO21/22 400kHz, 기어비 82로 구현됐다. `vTaskDelayUntil`·`micros()` 실측 dt 반영 뒤 단거리 오도메트리가 줄자와 3% 안에서 일치해 3.4배 속도 부풀림은 해소됐다. 조향각 엔코더는 없어 개루프다. 남은 것은 좌·우 3m 개별 스케일, 정·역방향 부호, overflow·장시간 aliasing 재시험이다 | 임베디드·기구 B | 실차 주행 전 |
| TBD-HW-005  | ~~짐벌 서보·IMU~~ | **2026-07-30 폐기 — 짐벌 프로젝트 제외 확정(6.4).** 카메라·LiDAR는 차체 고정 마운트로 대체하며 별도 서보·head IMU가 필요 없다 | — | 폐기 |
| TBD-HW-006  | 최종 크기·중량  | 3D 모델·부품 배치 완료 후 실측값                | 임베디드·기구 B | 2주차 초 |
| TBD-HW-007  | E-Stop 부품     | **2026-07-30 전원 구조 확정**: 12V 배터리(모터 전용) 단일 레일이 BTS7960 공통 전원을 급전(21.4). **2026-08-01 차동 구동 전환으로 공통 전원 대상은 BTS7960 2개**. **2026-08-06 차단 대상에 조향 서보 추가** — `BTS7960 ×2 + DS51150 서보`의 합산 최대 전류(구동 stall + 서보 stall 동시)를 감당해야 한다. **2026-08-11 미도입으로 종료**: 부품 모델·차단 정격을 확정하지 못했고 시연 차량에 장착되지 않았다. 명세 여러 곳이 이를 「안전 우선순위 최상위」로 적고 있었으나 실재하지 않아 전부 정정했다(03장 34-10). 실제 정지 수단은 watchdog 300ms·수동 TTL 250ms·collision_monitor·관제 정지이며 하드웨어 차단은 배터리 연결 분리뿐이다 | 임베디드·기구 B | **미도입 종료** |
| TBD-HW-008  | 전륜 서보 조향 보정 | DS51150-12V 서보와 타이로드, 모터 ESP32 PWM 경로, 조향 프로토콜은 구현됐다. 실측값은 `L=0.683m`, 바퀴 `δ_max=22°`, 서보 중립 145°, 최대 오프셋 ±55°이며, 펌웨어 `STEERING_MAX_MDEG=22000`·`SERVO_MAX_OFFSET_DEG=55`로 2.5:1 링키지 비를 반영했다. `steering_measure.py` 원주행으로 `R_min` 좌 1.37m·우 1.76m를 확정해 Smac에는 안전측 1.8m를 넣었다. 남은 것은 좌·우 5점 각도표·히스테리시스·슬루레이트·stall 전류 인수다. `control_page.h`의 미리보기용 `STEERING_MAX_DEG=30`과 `sentinel_drive` 단위 시험의 30° fixture는 현재 22° 코드와 불일치하므로 후속 동기화가 필요하다(실제 수동 명령은 정규화 값을 펌웨어 22° 상수로 매핑한다) | 임베디드·기구 B | 종료 시 일부 미완료 |
| TBD-HW-009  | ESP32 제어 보드 실배선 인수 | ESP32 WROOM-32 2개와 CH340 USB-UART를 사용한다. 모터 보드는 BTS7960 ×2·GPIO18 서보·선택적 모바일 Wi-Fi를, 센서 보드는 MT6701 ×2·MPU6050·DHT-11·HC-SR04 ×2를 담당한다. Jetson에는 물리 USB 경로 기반 `/dev/sentinel_mcu_motor`·`/dev/sentinel_mcu_sensor` 별칭으로 연결한다. 코드 핀맵은 확정됐고 남은 것은 전방 초음파 도통, 모터 보드 USB 물리 포트, 5V ECHO 레벨 변환과 전체 배선 인수다 | 임베디드·기구 B | 실차 주행 전 |
| TBD-HW-010  | 전방·후방 초음파 센서 | **구현·장착 확인(2026-08-09)**: HC-SR04 2개. 전방 `TRIG=GPIO18`/`ECHO=GPIO39`, 후방 `TRIG=GPIO5`/`ECHO=GPIO36`; 센서 ESP32가 전방 다음 후방을 순차 측정하고 `/range/front`·`/range/rear`까지 발행한다. 후방 위치도 URDF에 반영했다. 전방은 빈 공간 오측 때문에 `PROXIMITY_STOP_ENABLED=false`, 후방은 판정식 미포함이라 현재 둘 다 관측 전용이다. **남은 값**: 전방 센서·배선 개선 후 오측 0회 확인, 후방 유효 거리·장착 보정·정지거리, 방향별 보호 정책 채택 여부 | 임베디드·기구 B | 실차 후진 시험 전 |
| TBD-HW-011  | 듀얼 ESP32 속도 폐루프·초음파 릴레이 | **현재 구현**: MT6701은 센서 ESP32, BTS7960은 모터 ESP32에 있고 Jetson이 두 USB 채널을 중계한다. `esp32_motor_bridge_node`의 상승 엣지 3회·유지 중 200ms `STOP_COMMAND` 중계는 구현돼 있다. 다만 센서 펌웨어가 전방 오측 때문에 `protective_stop`을 항상 0으로 내고 후방도 판정하지 않아 현재 중계는 발동하지 않는다. **남은 값**: 전방 오측 제거 후 플래그 복원, 보호 신호부터 PWM 0까지 왕복 지연, 수동 모드 재확인 UX, 후방 정지 정책. 지연이 300ms를 넘으면 ESP32 간 직접 UART를 검토한다 | 임베디드·기구 B | 실차 안전 시험 전 |
| TBD-HW-012  | ~~차체 IMU 모델·배선·장착 오프셋~~ → 장기 검증 | **하드웨어 항목 해소**: MPU6050(GY-521), I2C `0x68`(`AD0=GND`), GPIO21/22 400kHz, 3.3V, MT6701과 버스 공유. ±500dps·±4g·DLPF 44Hz·100Hz 샘플, Jetson 약 90Hz 수신. REP-103 축과 위치 `(0.233, −0.010, 0.215)m`, roll −2.3°·pitch +9.3°를 확인해 URDF에 반영했다. 센서 루프 주기 수정 후 90° 회전에서 EKF 89.02°(오차 1.1%), 정지 bias +0.014°/s, 12초 yaw 드리프트 0.14°를 확인했다. **남은 것은 공분산·모터 진동 조건과 360° 원주행 장기 재현성**이다 | 임베디드·ROS2/주행 A | 종료 시 일부 미완료 |
| TBD-CAM-001 | ~~BRIO 100 ROS 2 연동~~ | **해소:** `/dev/video0` 1280×720 MJPEG 29.93FPS, `usb_cam` 단일 오픈과 X4 Pro `/scan` 동시 구동, HW MJPEG 디코딩 10분 연속 검증을 PoC-A에서 완료했다 | AI A | 해소 |
| TBD-SW-001  | ~~추가 학습 YOLO26n 배포~~ | **해소:** 파인튜닝은 미채택했고 사전학습 `yolo26n`·`yolo26n-pose`를 사용한다. Jetson 프로파일은 두 모델 모두 TensorRT `.engine`만 허용하며 통합 FPS·자원 실측도 완료했다. 가중치·엔진 해시와 라이선스는 릴리스 묶음에서 관리한다 | AI A | 해소 |
| TBD-NAV-001 | Frontier 구현   | 패키지 또는 자체 노드, 파라미터. **2026-08-04: Nav2 채택 방향 확정** — 젯슨에 `navigation2 1.1.20`·`robot_localization 3.5.4`가 이미 설치돼 있고, 명세가 Nav2를 하중 받는 자리 세 곳(23.3 도달성 판정, 23.4 정체 복구, 23.5 복귀 목표)에 쓰고 있다. **2026-08-04 구성 확정(S15P11A301-235)**: planner NavFn, controller rotation shim → Regulated Pure Pursuit, `allow_unknown=false`(23.3 도달성 판정의 근거), costmap 2개(global=map/static, local=odom/scan), cmd_vel 은 `/cmd_vel_nav`(안전 체인 입력). 파라미터는 `sentinel_bringup/config/nav2.yaml`, 상세 근거는 24.1 「구성 확정」. 축소판(순찰 시퀀스) 판단만 S15P11A301-172 에 남는다. **2026-08-06 전륜 서보 조향 전환으로 일부 재검토가 열렸다**(24.1 「전륜 조향 전환의 Nav2 영향」): rotation shim **제거**, `behavior_server` 의 `spin` **비활성**, `backup` 은 저속·거리 제한(후방 초음파 없음), RPP `use_rotate_to_heading: false`, `minimum_turning_radius` 신설. **2026-08-08 후방 초음파 하드웨어 추가 착수(S15P11A301-324, TBD-HW-010)** — 배선·캘리브레이션·안전 체인 통합이 끝나기 전까지는 `backup`의 "후방 초음파 없음" 제약을 그대로 유지한다. 통합이 끝나면 이 제약 완화(거리 상한 확대 등) 여부를 재검토한다. **2026-08-09 planner 확정(S15P11A301-358): Smac Hybrid-A\* 전환.** 판단 기준이던 `R_min` 실측이 들어왔고(341: 좌 1.37m·우 1.76m), 두 실기동에서 NavFn 이 회전반경을 모른 채 후방·측면 목표로 못 따라갈 경로를 내는 대가가 그대로 관측됐다(조향 상한 고착, 좌우 진동 — r60·r61). `SmacPlannerHybrid` + REEDS_SHEPP(`minimum_turning_radius 1.8`) + RPP `allow_reversing` 구성. **후진 계획 동작 확인**(계획 실측: 비용 0 회랑 정후방 1.1m → 전원 후진 포즈의 일직선 경로). 한 차례 「후진 불가」로 오판했다가 정정 — 시험 목표가 costmap 벽 팽창 안이었다. 남은 검증은 실주행(cusp 방향전환 데드타임 × RPP). 후진 도입으로 collision_monitor 정지·감속 구역도 후방으로 확장했다(`safety.yaml`). RPP·`allow_unknown=false`(23.3 결합 유지)·costmap 구성·remap 은 그대로 유효하다 | ROS2·주행 A    | 2주차 |
| TBD-AUD-001 | 오디오 입출력 검증 | 스피커는 블루투스(A2DP) **확정**, 마이크는 차체 고정 카메라(BRIO 100) 내장 **잠정** — 주행 소음 환경 STT 인식률, 미달 시 USB 마이크 전환 | AI B | 1주차 |
| TBD-AUD-002 | 음성 모델 선정 | **2026-08-05 확정**: STT `Qwen3-ASR-1.7B`(L40S FastAPI), LLM `gpt-5.4-mini`(GMS 구조화 전용), 안내는 승인된 사전 녹음 WAV. Jetson에는 Silero VAD와 클라이언트만 둔다. 남은 값은 실제 BRIO 소음 코퍼스의 오류율과 전체 스택 E2E·자원 실측 | AI B | 소음 시험 |
| TBD-AUD-003 | ~~음성 세션·Mission Manager ROS 2 연동 계약~~ | **해소:** `sentinel_voice.ros_node`가 `/perception/encounter`의 `APPROACHED`에서 encounter당 한 번 세션을 시작하고 `/mission/status`로 중단 조건을 감시한다. 구조화 보고는 `/interaction/report`, 종료 통지는 안내 재생 뒤 `/mission/signal`의 `DIALOGUE_ENDED`로 발행한다. `voice.launch.py`와 `demo.launch.py(enable_voice=true)`에 연결됐다. 알려진 GMS gate 오류는 08장 33장에 별도 결함으로 남긴다 | AI B·ROS2/주행 A | 해소 |
| TBD-CAL-001 | 최종 주행값     | 구동은 PID를 도입하지 않고 실측 회귀 기반 개루프 속도→PWM 매핑으로 종료했다. 최종 운용값은 순항 0.30m/s(실속도 99%), 접근 0.25m/s, `R_min` 좌 1.37m·우 1.76m, Smac 안전측 1.8m다. **2026-08-06 전륜 서보 조향 전환으로 「제자리 회전 대칭」 항목을 폐기하고 「좌·우 선회 대칭」으로 대체했다**(제자리 회전 불가, 6.3). **2026-08-05 초음파 임계 확정(S15P11A301-272)**: 전방 실측 0.15~0.18m였으나 빈 공간 오측 때문에 최종 시연에서는 보호 판정을 비활성화했다. **2026-08-09 IMU·EKF 정확도 확정(S15P11A301-363)**: 90° 회전에서 EKF 89.02°(오차 1.1%), 정지 자이로 bias +0.014°/s, 정지 12초 yaw 드리프트 0.14°. **남은 값은 좌·우 장거리 스케일, 실제 정지거리, 조향각↔yaw rate·히스테리시스, 후방 초음파 임계와 장기 360° 재현성**이다 | ROS2·주행 A | 종료 시 일부 미완료 |
| TBD-CAL-002 | **바퀴 지름·후륜 트랙폭 `W`** | **2026-08-04 신설(CAL-001에서 분리).** 바퀴 지름이 없으면 `vehicle_kinematics`(S15P11A301-234)의 지령을 실제 속도로 환산할 수 없다. 좌·우 거리 스케일도 함께 잰다. **2026-08-06 전륜 서보 조향 전환으로 `W` 의 역할이 바뀌었다** — 차동 역운동학의 필수 파라미터에서 빠졌고(그 자리는 휠베이스 `L`, TBD-HW-008이 받는다), 이제는 ① 선회 중 후륜 스크럽 정량화와 ② 전자 차동 보정을 도입할지 판단하는 데 쓴다. **전자 차동은 기본 미적용으로 확정했다**(6.3 — 초기에는 좌·우 동일 속도를 주고, 조향 중 타이어 스크럽·편향을 실측한 뒤 필요하면 추가한다). 즉 `W` 는 여전히 재지만 이제 주행을 막는 값은 아니다 | 임베디드·기구 B | 1주차 중 |
| TBD-VID-001 | 영상 저장 매체  | **2026-07-28 확정**: NVMe 미장착 상태로 진행하고, 링 버퍼와 `pending`을 microSD 단일 볼륨에 둔다(부록 D 경로). 현행 1500kbps 영상+64kbps 오디오에서 `pending` 보존 상한 30분은 약 352MB+컨테이너 오버헤드이며 초과 시 업로드 완료분부터 삭제한다(32-5). 링 버퍼는 8초 순환이라 누적하지 않으므로 약 1.6MB로 고정되고, 용량 위험은 `pending`에만 있다. 남은 제약은 용량이 아니라 microSD 쓰기 내구성이며, 시연 범위를 넘는 상시 녹화는 NVMe 장착을 전제로 한다 | 백엔드·영상 A | 확정 |
| TBD-VID-002 | ~~목표 비트레이트~~ | **확정:** PoC-B와 통합 부하 실측 뒤 관제 인코딩을 1280×720, 15FPS, 1500kbps, key-int 15로 고정했다. 카메라 캡처는 29.93FPS를 유지하고 인코딩 분기에서만 15FPS로 낮춘다 | 백엔드·영상 A | 확정 |
| TBD-OPS-001 | systemd 서비스화 | **2026-08-04 정정: 유닛 1개 구현됨** — `scripts/systemd/sentinel-demo.service`(S15P11A301-156), 기본 비활성. 37-3의 9개로 쪼개는 구조는 채택하지 않고 단일 진입점(`demo_up.sh` → `demo.launch.py`)으로 갔다. **2026-07-28 MVP 범위 외로 결정.** MVP가 자동 복구를 구현하지 않으므로 `Restart` 정책의 이점이 사라지고 남는 것은 부팅 시 자동 시작 편의뿐이다. 개발 중에는 `journalctl`과 `Restart`가 오히려 디버깅을 방해한다. **2026-08-06 추가(S15P11A301-294): 그 방해가 실제로 발생했다** — 시연 준비로 유닛을 `enable`한 뒤 `demo_down.sh`로 내렸는데 `Restart=on-failure`가 5초 뒤 스택을 되살렸다(launch는 SIGINT에 exit 1로 끝나므로 systemd가 고장으로 읽는다). `demo_down.sh`가 유닛 상태를 보고 `systemctl stop`을 타도록 고쳤다. 서비스를 끈 개발 구성에서는 실행을 `scripts/demo_up.sh`로 사람이 한다. 38장 인수 시험에 systemd를 요구하는 항목이 없고, 안전은 ESP32 watchdog이 담보하므로 안전 영향도 없다. 도입한다면 시연 리허설 전에 `sentinel-ros-bringup`과 `sentinel-media` 둘만 만든다. 없는 노드의 유닛을 미리 만들면 S15P11A301-125에서 지운 빈 CI 스테이지와 같은 함정이 된다. **2026-08-06 추가(S15P11A301-306): `enable` 해 둘 때의 대가가 하나 더 드러났다** — 이 젯슨은 RTC 백업 배터리가 없어 부팅마다 시계가 두 번 점프하고(epoch → 마지막 기록 시각 → NTP 실동기), 유닛이 그 사이에 뜨면 두 번째 점프가 TF 캐시·스트림 타임아웃·ESP32 워치독을 한꺼번에 부순다. 유닛을 `time-sync.target` 뒤로 보내고 `systemd-time-wait-sync` 를 `enable` + `TimeoutStartSec` 상한을 걸어 막았다(06장 37-3-1). 손으로 `demo_up.sh` 를 부르는 개발 구성에는 이 문제가 없다 — 그때는 시각이 이미 맞아 있다 | 백엔드·영상 A | MVP 이후 |
