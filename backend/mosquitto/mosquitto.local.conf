# 로컬 개발용 Mosquitto 설정. 운영 설정은 mosquitto.prod.conf 를 사용한다.
#
# Mosquitto 2.0 은 listener 를 명시하지 않으면 127.0.0.1 에만 바인딩하므로,
# 컨테이너 밖(호스트에서 실행하는 스프링)에서 붙으려면 아래 선언이 필요하다.

listener 1883
allow_anonymous true

persistence false
log_dest stdout
