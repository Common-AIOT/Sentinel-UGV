/**
 * 사이드바 폭 (S15P11A301-259).
 *
 * 전에는 메인이 무엇이냐에 따라 폭이 달라졌다 — 영상이 메인이면 16:9 상자가
 * 먹고 남은 폭(실측 208px), 지도가 메인이면 고정 308px 이었다. 같은 페이지에서
 * 패널이 넓어지고 좁아지는 것으로 보였다. 두 값의 평균으로 고정한다.
 *
 * **관제 화면 밖으로 꺼냈다 (S15P11A301-318).** 토스트 폭도 이 값을 따라야 하는데
 * (아래 참고) 토스트는 Providers 가 앱 전체에 하나만 두므로 page.tsx 안의 상수를
 * 쓸 수 없었다. 숫자를 양쪽에 적으면 한쪽만 고치는 날이 온다 — 같은 이유로
 * page.tsx 는 슬롯 높이를 폭에서 유도한다.
 */
export const SIDEBAR_WIDTH = 260;
