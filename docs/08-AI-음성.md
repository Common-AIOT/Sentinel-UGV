> Sentinel UGV 통합 명세서 v2.0의 일부 — 문서 규칙·버전·변경 이력은 [README](README.md) 참조
>
> **문서 책임:** 피해자 음성 상호작용의 최종 설계, GPU ASR 서버, Jetson 실행,
> 안전 계약, 실측 결과와 미채택 이력을 한곳에서 관리한다.

# 33. 피해자 음성 상호작용 설계 [확정]

## 33.1 한 줄 결론

현재 운영 구조는 **Jetson 로컬 VAD → L40S FastAPI Qwen3-ASR → GMS
`gpt-5.4-mini` 구조화 → 규칙 기반 위험도 → 승인된 사전 녹음 WAV 재생**이다.

```mermaid
flowchart LR
    encounter["VISION Encounter"] --> guide["사전 녹음 안내"]
    guide --> mic["BRIO 100 마이크"]
    mic --> vad["Jetson Silero VAD"]
    vad --> asr["L40S FastAPI\nQwen3-ASR-1.7B"]
    asr --> guard["환각·빈 전사 가드"]
    guard --> gms["GMS gpt-5.4-mini\n3필드 구조화"]
    gms --> rule["규칙 기반 위험도"]
    rule --> report["관제 보고 인계"]
    report --> closing["종료 안내 후 탐사 재개"]
```

| 단계 | 최종 선택 | 배치 |
|---|---|---|
| 입력 | BRIO 100 내장 마이크 | Jetson |
| 안내 | 승인된 사전 녹음 WAV 6개 | Jetson |
| VAD | Silero VAD | Jetson |
| STT | `Qwen/Qwen3-ASR-1.7B` | L40S Device3 FastAPI |
| 정보 추출 | GMS `gpt-5.4-mini` | 외부 API |
| LLM 장애 폴백 | 제한된 키워드 파서 | Jetson |
| 위험도 | `voice-risk-v1.1` 명시 규칙 | Jetson |
| 출력 | 블루투스 A2DP 스피커 | Jetson |

동적 TTS, Jetson 로컬 Whisper, Jetson 로컬 LLM은 운영 경로가 아니다. DeepFilterNet은
관제 청취본 후처리용이며 STT 입력 앞에 연결하지 않는다.

## 33.2 안전 경계

- LLM은 의료 진단, 구조 우선순위 확정, 주행 명령을 하지 않는다.
- LLM은 인원 수, 이동 가능 여부, 긴급 상태 언급만 정해진 JSON으로 추출한다.
- 위험도는 추출값을 입력으로 하는 명시적 규칙이며 관제 검토용 참고값이다.
- 로봇은 요구조자에게 위험 등급을 말하지 않는다.
- STT 실패를 무응답으로 기록하지 않는다.
- 네트워크가 끊긴 상태에서는 신규 음성 세션을 시작하지 않는다.
- 이미 STT가 끝난 뒤 GMS만 실패한 경우에만 키워드 폴백으로 축소 보고한다.
- 원음, API 키와 인증 헤더를 일반 애플리케이션 로그에 남기지 않는다.

## 33.3 대화 상태 머신

질문은 부상 우선 순서로 한 번씩 진행한다.

| 단계 | 파일 | 승인 안내 문구 |
|---|---|---|
| `INTRO` | `guide_intro.wav` | 구조 로봇입니다. 들리면 대답해 주세요. |
| `URGENT` | `guide_ask_urgent.wav` | 다친 곳이 있으십니까? |
| `MOBILITY` | `guide_ask_mobility.wav` | 움직일 수 있습니까? |
| `COUNT` | `guide_ask_count.wav` | 주변에 다른 인원이 있습니까? |
| `RETRY_NO_RESPONSE` | `guide_retry_no_response.wav` | 제 말이 들린다면 대답해주십시오. |
| `REPORT_SUCCEEDED_DEPARTURE` | `guide_report_succeeded_departure.wav` | 구조 요청이 전달되었습니다. 다시 탐색을 시작합니다. |

INTRO에서 무응답이면 한 번만 재질문한다. 이후 값이 확정되지 않아도 같은 질문을 반복하지
않고 `UNKNOWN`으로 진행해 대화를 끝낸다.

### 턴 결과 4분류

| 상태 | 음성 감지 | STT | 구조화 | 의미 |
|---|---:|---:|---:|---|
| `NO_RESPONSE` | 아니오 | — | — | 실제 무응답 |
| `VOICE_DETECTED_STT_FAILED` | 예 | 실패 | — | 사람은 답했지만 전사 실패 |
| `ANSWER_UNSTRUCTURED` | 예 | 성공 | 실패 | 문장은 있으나 필드 확정 불가 |
| `ANSWER_STRUCTURED` | 예 | 성공 | 성공 | 필드 기록 가능 |

반문과 맥락을 고려한다. 예를 들어 “다리 다쳤는데 움직일 수 있겠냐고요”는 질문 맥락상
이동 불가를 뜻하므로 `mobilityStatus=NO`다. 단순 키워드 `움직일 수`만 보고 `YES`로
판정하지 않는다.

## 33.4 보고 계약

세션 보고의 기준 필드는 다음과 같다.

```json
{
  "responseScope": "GROUP",
  "anyResponseDetected": true,
  "reportedResponsiveCount": 2,
  "reportedCountStatus": "SELF_REPORTED_GROUP_COUNT",
  "countConfidence": null,
  "mobilityStatus": "NO",
  "urgentConditionReported": "YES",
  "operatorReviewRequired": true,
  "terminationReason": "NORMAL"
}
```

| 필드 | 책임 주체 |
|---|---|
| `anyResponseDetected` | VAD·상태 머신 |
| `reportedResponsiveCount` | GMS 또는 키워드 추출 결과 |
| `mobilityStatus` | GMS 또는 키워드 추출 결과 |
| `urgentConditionReported` | GMS 또는 키워드 추출 결과 |
| `terminationReason` | 상태 머신 |
| `riskLevel`, `riskReasons` | 규칙 엔진 |
| 최종 구조 판단 | 관제자 |

`UNKNOWN`은 질문했지만 값이 확정되지 않았다는 뜻이다. `null`은 해당 필드에 값 자체가
적용되지 않거나 제공되지 않았다는 뜻이다. 여러 사람이 있는 경우 자동 화자 분리를 하지
않고 `responseScope=GROUP`으로 전달한다.

관제 ACK를 기다리는 구조는 사용하지 않는다. 보고는 전송 어댑터의 성공·대기 상태로 인계하고,
종료 안내는 한 번만 재생한 뒤 Mission Manager 계약에 따라 탐사를 재개한다.

## 33.5 GPU ASR 서버

### API 계약

```http
GET /health
POST /v1/asr
Authorization: Bearer <ASR_API_KEY>
Content-Type: multipart/form-data
```

지원 입력은 WAV/FLAC/OGG, mono/stereo, 8~48kHz이며 기본 제한은 30초·16MiB다. 공유
GPU 동시 추론 기본값은 1건이다.

| HTTP | 오류 코드 | 클라이언트 처리 |
|---:|---|---|
| 401 | `UNAUTHORIZED` | 키·환경 설정 확인, 재시도 안 함 |
| 413 | `AUDIO_TOO_LARGE`, `AUDIO_TOO_LONG` | 입력 계약 수정 |
| 415 | `UNSUPPORTED_AUDIO_TYPE`, `INVALID_AUDIO` | 녹음·인코딩 확인 |
| 422 | `UNSUPPORTED_LANGUAGE`, `UNSUPPORTED_SAMPLE_RATE` | 요청 계약 수정 |
| 429 | `ASR_OVERLOADED` | 제한 재시도 후 STT 실패 기록 |
| 503 | `MODEL_NOT_READY`, `MODEL_INFERENCE_FAILED` | health 확인 후 제한 재시도 |

오류 응답은 `error.code`, `message`, `request_id`, `retryable`을 포함한다. Jetson은 어떤
서버 오류도 `NO_RESPONSE`로 바꾸지 않는다.

### L40S 실행

```bash
cd /path/to/S15P11A301/ai/voice
python3.12 -m venv .venv-gpu-asr
source .venv-gpu-asr/bin/activate
python -m pip install -U pip
python -m pip install -r asr_server/requirements.txt

cp asr_server/.env.example .env.gpu-asr
set -a
source .env.gpu-asr
set +a
python -m asr_server
```

`ASR_CUDA_VISIBLE_DEVICES=3`은 프로세스 내부 `cuda:0`을 물리 Device3에 연결한다.

```bash
nvidia-smi -i 3
curl -sS http://127.0.0.1:18100/health
```

기본 바인딩은 `127.0.0.1:18100`이다. Jetson이 직접 접근하는 격리 개발망 시험에서만
`0.0.0.0`과 평문 HTTP 예외를 사용한다. 장기 운영은 TLS reverse proxy, VPN 또는 승인된
SSH 터널 뒤에 둔다. 포트를 인터넷에 직접 공개하지 않는다.

### 보안

- Bearer 또는 `X-API-Key` 인증을 필수로 한다.
- `.env.gpu-asr`와 실제 키는 Git에 넣지 않는다.
- 업로드 임시 파일은 성공·실패와 무관하게 요청 종료 시 삭제한다.
- 원음과 전사문을 ASR 서버 로그에 남기지 않는다.
- 타 팀이 공유 GPU를 사용 중이면 프로세스를 임의로 종료하지 않는다.

## 33.6 Jetson 실행 가이드

### 실제 경로

```text
저장소: /home/orin/projects/S15P11A301
가상환경: /home/orin/projects/S15P11A301/.venv
음성 코드: /home/orin/projects/S15P11A301/ai/voice
환경 파일: /home/orin/projects/S15P11A301/ai/voice/.env
```

다른 경로에 clone했다면 명령 전체에서 `/home/orin/projects/S15P11A301`만 실제 경로로
바꾼다. 존재하지 않는 별도 배포 폴더를 만들 필요는 없다.

### 최초 준비

```bash
cd /home/orin/projects/S15P11A301
git pull --ff-only origin develop
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -U pip
python -m pip install -r ai/voice/requirements-jetson-remote.txt

cp ai/voice/.env.example ai/voice/.env
chmod 600 ai/voice/.env
```

`ai/voice/.env`에서 다음 값을 실제 값으로 바꾼다.

```dotenv
GMS_KEY=<팀 GMS 키>
SENTINEL_LLM=gpt-5.4-mini
SENTINEL_ASR_BASE_URL=http://70.12.130.105:18100
SENTINEL_ASR_API_KEY=<GPU ASR 서버가 생성한 팀 전용 키>
SENTINEL_ASR_ALLOW_INSECURE_HTTP=1
```

`SENTINEL_ASR_ALLOW_INSECURE_HTTP=1`은 격리 개발망 시험용이다. 공개망 운영 값으로
복사하지 않는다.

### 연결과 환경 확인

```bash
curl -sS http://70.12.130.105:18100/health

cd /home/orin/projects/S15P11A301/ai/voice
/home/orin/projects/S15P11A301/.venv/bin/python -m tools.check_env
/home/orin/projects/S15P11A301/.venv/bin/python -m tools.check_env --load
```

`--load`는 원격 ASR 인증·실제 전사, Silero VAD 로드와 GMS 호출까지 확인한다. 첫 실행은
Silero 가중치를 내려받아 시간이 더 걸릴 수 있다.

### 오디오 확인

```bash
arecord -l
aplay -l
pactl list cards short
pactl list sinks short

arecord -D <BRIO_DEVICE> -f S16_LE -r 16000 -c 1 -d 5 /tmp/brio-test.wav
aplay /tmp/brio-test.wav
```

블루투스 스피커는 출력 전용 A2DP profile을 사용한다. HFP/HSP 마이크 모드는 입력 품질을
낮추므로 사용하지 않는다. 안내 음성 재생 중에는 마이크 입력이 이를 사용자 응답으로 다시
인식하지 않는지 확인한다.

### 파이프라인 단독 실행

```bash
cd /home/orin/projects/S15P11A301/ai/voice
/home/orin/projects/S15P11A301/.venv/bin/python -u -m sentinel_voice.pipeline
```

정상 실행 시 VISION 트리거, 질문별 `[PLAY]`, `[STT]`, 세션 보고, 위험도 참고값,
종료 상태와 관제 보고 상태가 순서대로 출력된다.

### ROS 2 음성 노드

```bash
source /opt/ros/humble/setup.bash
cd /home/orin/projects/S15P11A301
source install/setup.bash

cd ai/voice
/home/orin/projects/S15P11A301/.venv/bin/python -u -m sentinel_voice.ros_node
```

Encounter와 임무 상태 토픽을 감시한 뒤 테스트 Encounter를 한 번만 발행한다. 실제 토픽명과
메시지 타입은 통합 launch 및 통신 명세를 기준으로 하며, 음성 노드가 같은 Encounter에서
세션을 중복 시작하지 않는지 확인한다.

## 33.7 검증 순서와 합격 기준

다음 순서를 건너뛰지 않는다.

1. GPU 서버 `/health`가 `ready=true`, `backend=qwen3-asr`인지 확인한다.
2. Jetson에서 GPU 서버 IP와 포트에 연결되는지 확인한다.
3. `.env`와 사전 녹음 WAV 형식을 검사한다.
4. BRIO 녹음과 스피커 재생을 각각 확인한다.
5. 단위 테스트를 실행한다.
6. 음성 파이프라인 단독 E2E를 실행한다.
7. ROS Encounter 트리거를 연결한다.
8. YOLO·SLAM·Nav2·영상 송출과 함께 자원을 측정한다.
9. 위 기준선이 통과한 뒤 소음 조건을 측정한다.

```bash
cd /home/orin/projects/S15P11A301/ai/voice
python -m unittest discover -s tests -v
python -m tools.validate_guide_assets
python -m evaluation.pipeline_bench
python -m evaluation.jetson_resource_bench --help
```

### 육성 시나리오

| 시나리오 | 핵심 확인 |
|---|---|
| A 중증 | 긴급 `YES`, 이동 `NO`, 숫자·부정어 보존 |
| B 경상 | 긴급 `NO`, 이동 `YES` |
| C 완전 무응답 | `NO_RESPONSE`, 응답 없음과 서버 실패 구분 |
| D 음성은 있으나 STT 실패 | `VOICE_DETECTED_STT_FAILED` |
| E 모호한 답 | 임의 확정하지 않고 `UNKNOWN` |

### 정량 기준

| 항목 | 목표 또는 상태 |
|---|---|
| ASR quiet / SNR 10 / 5dB CER | 15% / 20% / 25% 이하 목표 |
| ASR 핵심 숫자·부정어 | 치명 왜곡 0건 목표 |
| GMS 스키마 | 유효 JSON 100% |
| GMS 위험한 반전 | 0건 |
| GMS P95 | 3초 이하 목표 |
| 전체 E2E | Jetson 실측값과 P50/P95 기록 필요 |
| 통합 자원 | OOM·열 스로틀링 없이 임무 지속 |

테스트 결과에는 commit, 장치, 전력 모드, sample·speaker·SNR, WER/CER, 핵심 슬롯,
GMS 상태, schema 유효성, 환각 슬롯, 단계별 monotonic timestamp를 함께 기록한다.

## 33.8 실측 결과와 모델 선정

### ASR: Qwen3-ASR-1.7B 채택

L40S Device3에서 동일 합성 음성 9건을 모델별 3회 실행한 결과다.

| 모델 | micro CER | micro WER | 핵심 표현 | 위험·안전 뒤집힘 | P50 / P95 | RTF | VRAM |
|---|---:|---:|---:|---:|---:|---:|---:|
| Qwen3-ASR-1.7B | **0.0000** | **0.0000** | 100% | 0 | 260.667 / 425.847ms | 0.1024 | 5,014MiB |
| faster-whisper turbo | 0.0291 | 0.0968 | 비교 열세 | 별도 기록 | — / 367.649ms | — | 2,170MiB |

Whisper turbo가 더 가볍고 약간 빠르지만 정확도와 핵심 표현 보존에서 Qwen이 우세해 운영
모델로 선정했다. 이 코퍼스는 합성 스모크 규모가 작으므로 현장 마이크·소음 코퍼스에서
같은 결론이 유지되는지 별도 검증한다.

과거 KsponSpeech에서 로컬 Whisper small을 측정한 값은 clean CER 0.141, SNR 0dB CER
0.366이었다. 이는 한국어 일반 코퍼스 기준이지 재난 현장 성능이 아니며 현재 원격 Qwen
운영 성능으로 재사용하지 않는다.

### LLM: GMS gpt-5.4-mini 유지

프롬프트 v2 회귀에서 20문장 60필드 100%, 안전 일치 20/20, 위험·안전 반전 0건,
P95 1.78초를 기록했다. 외부 생성 표준 한국어 문항까지 확장한 평가에서도 비용·지연·안전
균형이 가장 좋아 운영 모델로 유지했다.

L40S 로컬 `Qwen3.5-4B` shadow는 P95 7,616.67ms로 GMS 2,017.06ms보다 느렸고 치명
오분류 1건이 있어 전환하지 않았다. Claude Opus 계열은 일부 정답이 개선됐지만 비용과 P95가
증가해 채택하지 않았다. 사투리는 현재 합격 범위 밖이며 표준 한국어와 일반 구어체만 보장한다.

### 안내 음성: 사전 녹음 유지

다국어 TTS 후보를 L40S에서 비교했으나 질문이 고정돼 있고 실제 운영 음성의 일관성·검수·즉시
재생이 더 중요하므로 기존 사전 녹음 자산을 유지했다. 런타임 TTS 모델은 설치하지 않는다.
생성 모델 비교 결과는 의사결정 이력일 뿐 운영 의존성이 아니다.

필수 자산은 `ai/voice/assets`의 intro, urgent, mobility, count, retry, closing 계열 WAV다.
배포 전에 `python -m tools.validate_guide_assets`로 형식과 누락을 검사하고 사람이 실제
스피커로 청취 검수한다.

### 메모리 결정

과거 Jetson 로컬 Whisper와 로컬 3B LLM 조합은 8GB 메모리에서 다른 로봇 기능과 경쟁했다.
로컬 `qwen2.5:3b`는 page cache 압박 시 GPU 로드 실패도 관측됐다. 현재는 STT 모델을 L40S,
LLM을 GMS로 이동하고 Jetson에는 VAD·오디오·HTTP 클라이언트만 남겨 메모리 위험을 줄였다.
다만 YOLO·SLAM·Nav2·영상까지 함께 실행한 통합 peak는 실제 Jetson에서 다시 측정해야 한다.

## 33.9 소음과 잡음 제거

DeepFilterNet은 **관제자가 듣는 녹음 후처리에는 채택**, **STT 전처리에는 미채택**한다.

| 관측 | 결과 |
|---|---|
| 합성 A/B 108조건 | 원본 승 58, denoise 승 16, 무승부 34 |
| CER 중앙값 | 원본 0.467, denoise 0.667 |
| 처리 비용 | 60초 음성 약 1초, RTF 0.017 수준 |
| 실제 동시 녹음 | 소음과 함께 약한 목소리 성분도 제거됨 |

소음이 없는 입력도 음소의 약한 성분을 깎을 수 있고, 전처리 뒤 조용한 환각이 생기면 기존
RMS·반복 가드가 잡지 못할 수 있다. 감쇠 상한을 조정해도 원본 STT를 넘지 못했으므로
`마이크 → denoise → STT` 경로를 만들지 않는다.

소음 평가는 Jetson 기준선 통과 후 수행한다. 실제 BRIO와 로봇 모터·팬·현장 소음을 동시에
녹음하고 quiet, SNR 10/5/0dB 또는 실측 조건별로 CER/WER, 숫자·부정어 보존, VAD 누락과
환각을 기록한다. 합성 소음만으로 현장 성능을 확정하지 않는다.

## 33.10 설계 변경과 미채택 이력

| 과거 검토안 | 현재 결정 | 이유 |
|---|---|---|
| Jetson Whisper small | 원격 Qwen3-ASR | 메모리 분리, L40S 정확도 우세 |
| faster-whisper large-v3/turbo | Qwen 유지 | 작은 지연 이득보다 핵심 표현 정확도 우선 |
| Jetson 로컬 3B LLM | GMS `gpt-5.4-mini` | 메모리·OOM·정확도 문제 |
| L40S Qwen3.5-4B LLM | GMS 유지 | 치명 오분류 1건, P95 열세 |
| 자유 대화형 LLM | 고정 질문+3필드 추출 | 예측 불가 발화와 의료 판단 방지 |
| 동적 TTS | 승인 사전 녹음 | 고정 질문, 검수 가능성, 지연·메모리 제거 |
| DeepFilterNet STT 전처리 | 원음 STT | 108조건과 실제 녹음에서 정확도 저하 |
| 공개 클라우드 Whisper API | 팀 GPU FastAPI | 무음 환각·보안·비용과 통제 범위 고려 |

과거 문서의 “STT·TTS 전부 Jetson 온디바이스” 또는 “배치 미확정” 문구는 폐기한다.
본 문서의 최종 구조가 현재 단일 기준이다.

## 33.11 장애 분류

| 증상 | 먼저 확인할 것 | 처리 |
|---|---|---|
| Jetson에서 연결 거부 | 서버 bind, 방화벽, 실제 IP·포트 | GPU 서버 health부터 복구 |
| health는 되지만 401 | 양쪽 API key 불일치 | `.env` 수정, 키 출력 금지 |
| 429 | 공유 GPU 동시성 포화 | 제한 재시도, 동시성 임의 증가 금지 |
| 503 | 모델 load·CUDA·VRAM | `nvidia-smi -i 3`, 서버 health 확인 |
| 마이크 무음 | ALSA 장치 번호·BRIO 입력 | 5초 직접 녹음부터 재검증 |
| 안내 음성이 응답으로 인식 | 스피커 방향·에코 가드 | 물리 배치와 청취 시작 시점 조정 |
| GMS 실패 | 호스트·키·timeout | STT 완료 건만 키워드 폴백 |
| 이동 반문이 UNKNOWN | transcript와 prompt 회귀 | 부정·반문 테스트 추가, 임의 키워드 YES 금지 |

## 33.12 남은 검증

- BRIO 실제 화자 3명 이상, 각 10발화 이상으로 현장 코퍼스를 만든다.
- quiet와 실제 로봇 소음에서 Qwen3-ASR CER/WER, 핵심 슬롯과 P50/P95를 측정한다.
- GPU 공유 부하 상태에서 429와 timeout 복구를 확인한다.
- Jetson 전체 스택의 RAM, CPU/GPU, 온도, 전력과 E2E 지연을 측정한다.
- Bluetooth 재연결과 사전 녹음 6개 청취·에코 검수를 완료한다.
- 실제 관제 전송 어댑터를 연결하고 보고 중복·누락을 확인한다.
- 사투리와 다국어는 별도 요구사항·코퍼스가 생긴 뒤 합격 범위를 확장한다.

## 33.13 정량 측정 기록

이 절은 모델 선정과 구조 변경에 사용한 수치를 보존한다. 측정 시점의 구성과 현재 운영
구성을 구분해야 한다. 특히 과거 Jetson RAM 실측에는 로컬 Whisper small이 포함되어 있으며,
현재 원격 Qwen3-ASR 구성의 Jetson RAM으로 해석하면 안 된다.

### 33.13.1 측정 상태 요약

| 영역 | 측정 상태 | 현재 결정에 사용하는 방식 |
|---|---|---|
| L40S ASR 후보 비교 | 합성 스모크 9건 × 모델별 3회 완료 | Qwen3-ASR 후보 선정 근거 |
| 실제 BRIO ASR | Jetson 단독 E2E 확인, 정식 현장 코퍼스 미측정 | 최종 현장 성능은 조건부 |
| GMS 모델 비교 | 회귀 44건 및 외부 표준 한국어 38건 완료 | `gpt-5.4-mini` 선정 근거 |
| L40S 로컬 LLM shadow | 동일 46케이스 완료 | 로컬 Qwen3.5-4B 미채택 근거 |
| Jetson RAM | 과거 로컬 LLM 구성과 GMS+로컬 Whisper 구성 완료 | 원격화 결정의 이전/중간 기준선 |
| 현재 원격 ASR Jetson RAM | 전체 로봇 스택 통합 측정 대기 | 실제 Jetson에서 신규 측정 필요 |
| 공개 한국어 STT | KsponSpeech 2,471발화 × 4소음 × 2프롬프트 완료 | 과거 Whisper 기준선·방법론 참고 |
| 잡음 제거 | 합성 108조건과 실제 동시 녹음 1건 완료 | STT 전처리 배제 근거 |

### 33.13.2 L40S ASR 후보 비교

측정 환경은 NVIDIA L40S Device3 46,068MiB, driver 570.211.01, CUDA Driver API
12.8이다. 한국어·영어 합성 발화와 무음·잡음으로 구성된 9개 스모크 케이스를 모델별
3회 실행했다.

| 모델 | micro CER | micro WER | 핵심 표현 | 위험→안전 | P50 | P95 | RTF | VRAM |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Qwen3-ASR-1.7B | **0.0000** | **0.0000** | 100% | 0 | **260.667ms** | 425.847ms | **0.1024** | 5,014MiB |
| faster-whisper large-v3 | 0.0097 | 0.0645 | 100% | 0 | 417.319ms | 509.149ms | 0.1607 | 3,528MiB |
| faster-whisper large-v3-turbo | 0.0291 | 0.0968 | 100% | 0 | 302.578ms | **367.649ms** | 0.1134 | **2,170MiB** |

CER·WER는 발화별 평균이 아니라 전체 편집 수를 전체 정답 단위로 나눈 micro 값이다.
Turbo는 VRAM과 P95에서 유리했지만 Qwen이 정확도에서 우세해 운영 모델로 선정됐다.

무음·잡음에서는 세 모델 모두 헛문장을 생성한 사례가 있었다.

| 모델 | 무음 예시 | 잡음 예시 |
|---|---|---|
| Qwen3-ASR | `그렇죠.` | `그렇지.` |
| large-v3 | `한글자막 by 한효주` | `시청해주셔서 감사합니다.` |
| large-v3-turbo | `다음 영상에서 만나요.` | `다음 영상에서 만나요.` |

따라서 모델 정확도만으로 무음 환각을 방어하지 않는다. Jetson 로컬 VAD, 원본 RMS,
빈 전사·반복·에코 가드를 함께 유지한다. 합성 9건은 후보 선별용이며 실제 화자·BRIO·잔향과
약한 발성을 대표하지 않는다.

현장 승격 기준은 최소 3명 × 핵심 10발화 × quiet/noisy × normal/weak, 총 120건이다.
위험→안전 뒤집힘 0건, 숫자·부정어·이동·긴급 표현 보존과 공유 GPU 부하 P95를 함께 본다.

### 33.13.3 GMS 정보 추출 모델 비교

프롬프트 v2 회귀 20문장 결과는 다음과 같다.

| 모델 | 파싱 | 슬롯 정확도 | 완전 일치 | 환각 슬롯 | 위험→안전 | P50 | P95 | 최대 | 평균 크레딧 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `gpt-5.4-mini` | 20/20 | **60/60** | **20/20** | 0 | 0 | 0.95초 | **1.78초** | 2.59초 | 10.041 |
| Claude Haiku 4.5 | 20/20 | 59/60 | 19/20 | 0 | 0 | 1.01초 | 2.23초 | 5.05초 | 11.615 |

Mini는 고난도 변형 24회와 위 회귀 20회를 합쳐 완전 일치 44/44, 평가 슬롯
132/132, 파싱 실패·환각·위험→안전 오판 0건을 기록했다. 이는 당시 개발·회귀 세트
결과이며 일반적인 현장 정확도 100%를 뜻하지 않는다.

별도로 준비한 외부 표준 한국어 38문항의 1회 평가 결과는 다음과 같다.

| 모델 | 요청 성공 | 슬롯 정확도 | 완전 일치 | 환각 슬롯 | 위험→안전 | P50 | P95 | 최대 | 평균 크레딧 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| GPT-5.4 Mini | 38/38 | 112/114 (98.25%) | 36/38 (94.74%) | 1 | 0 | 1.13초 | **2.12초** | 3.48초 | **10.1586** |
| Claude Haiku 4.5 | 38/38 | 111/114 (97.37%) | 35/38 (92.11%) | 3 | 0 | 1.03초 | 2.17초 | 2.37초 | 11.6016 |
| Claude Opus 4.8 | 38/38 | **113/114 (99.12%)** | **37/38 (97.37%)** | 0 | 0 | 1.84초 | 3.33초 | 7.32초 | 66.5395 |

Opus는 Mini보다 완전 일치 1건과 슬롯 1개가 높았지만 평균 크레딧이 약 6.55배이고
P95가 약 1.21초 길었다. 세 모델 모두 이 표본에서 위험→안전 오판 0건이므로 비용과
지연을 포함한 운영 균형으로 Mini를 유지한다.

Mini의 외부 문항 오답은 다음 두 건이었다.

| caseId | 기대 | 실제 | 실패 원인 |
|---|---|---|---|
| `mobility-hesitation-fear` | 이동 `UNKNOWN` | 이동 `NO` | 신체 능력이 아니라 붕괴 공포로 주저하는 발화를 이동 불가로 과해석 |
| `pet-not-person` | 인원 `1` | 인원 `null` | 반려동물을 제외한 뒤 명시된 사람 한 명을 추출하지 못함 |

사투리 2문항은 팀 결정 후 공식 점수에서 제외했다. 현재 제품 합격 범위는 표준 한국어와
일반 구어체이며, 실제 BRIO 전사 오류가 섞인 블라인드 세트는 아직 남아 있다.

### 33.13.4 L40S 로컬 LLM shadow

`Qwen/Qwen3.5-4B`와 GMS Mini를 같은 현재 프롬프트·46케이스로 1회 비교했다.

| 지표 | Qwen3.5-4B 로컬 | GMS gpt-5.4-mini |
|---|---:|---:|
| 요청 성공률 | 100.00% | 100.00% |
| Schema 유효율 | 100.00% | 100.00% |
| 슬롯 정확도 | 85.51% | **88.41%** |
| 3필드 완전 일치 | 67.39% | **69.57%** |
| 환각 슬롯 | 16 | 15 |
| 안전 치명 오분류 | **1** | **0** |
| P50 | 7,579.27ms | **1,054.17ms** |
| P95 | 7,616.67ms | **2,017.06ms** |
| 최대 | 8,115.57ms | **2,824.06ms** |
| 모델 VRAM | 9,160MiB | L40S 미사용 |

로컬 모델은 Schema 100%를 통과했지만 치명 오분류 허용 기준 0건을 위반했고, P50이
GMS보다 약 7.2배 느렸다. 따라서 별도 LLM FastAPI 서버로 전환하지 않았다.

### 33.13.5 Jetson RAM 실측과 해석

#### 과거 로컬 LLM 구성

2026-07-24 Jetson Orin Nano 8GB, JetPack 6.2.1, 15W, 헤드리스 환경에서 측정했다.
구성은 Silero VAD + CPU int8 faster-whisper small + GPU qwen2.5:3b이며 TTS는 로드하지
않았다. `tegrastats`가 표시한 사용 가능 총량은 7.4GB였다.

| 상태 | RAM 실측 |
|---|---:|
| idle 헤드리스 | 약 1.8GB |
| VAD + 로컬 STT 상주, LLM 미로드 | 약 3.1~3.3GB |
| 로컬 3B LLM 보고 피크 | **5.62GB / 7.4GB** |
| 피크 시 여유 | 약 1.9GB |

비전·SLAM·Nav2가 쓸 수 있는 여유가 약 1.9GB뿐이어서 로컬 LLM을 제거하는 근거가 됐다.
당시 `free`가 1.3GB, buff/cache가 4.2GB, available이 5.0GB인 상태에서도 CUDA가
1.79GB 연속 버퍼를 할당하지 못해 OOM이 발생했다. Jetson nvgpu 할당에서 available만
보고 안전하다고 판단하면 안 된다는 관측이다.

#### GMS 전환 후, 로컬 Whisper가 남아 있던 구성

2026-07-30 GUI·PulseAudio를 켠 Jetson 15W 환경에서 BRIO 100과 Bluetooth A2DP를
사용해 시나리오 A 육성 세션을 측정했다. 코드는 `e36f910`, 구성은 CPU int8 Whisper
small + GMS `gpt-5.4-mini`였다.

| 항목 | 실측 |
|---|---:|
| 시작 RAM | 2,670MB |
| 최저 RAM | 2,568MB |
| 피크 RAM | **4,102MB / 7,620MB** |
| 피크 시 여유 | **3,518MB** |
| 세션 E2E | **76.8초** |

로컬 3B 구성의 5,620MB보다 약 1,518MB 낮아졌다. 그러나 이 값에는 지금은 제거된
로컬 Whisper small이 포함돼 있다.

#### 현재 원격 ASR 구성

현재 Jetson에는 Qwen3-ASR 가중치와 CTranslate2가 없다. Silero VAD, 오디오 처리,
HTTP 클라이언트와 상태 머신만 남는다. 따라서 현재 구성이 과거 4,102MB보다 낮을 것으로
예상되지만, **아직 같은 조건으로 다시 측정하지 않았으므로 절감량을 확정하지 않는다.**

다음 표를 실제 Jetson에서 채워 현재 기준선을 확정한다.

| 동시 구동 조합 | 총 RAM peak | available 최소 | 음성 E2E P95 | 비전 FPS | OOM/스로틀 | 판정 |
|---|---:|---:|---:|---:|---|---|
| 음성 원격 ASR 단독 | 미측정 | 미측정 | 미측정 | — | 미측정 | 대기 |
| 비전 + SLAM | 미측정 | 미측정 | — | 미측정 | 미측정 | 대기 |
| 비전 + SLAM + 음성 | 미측정 | 미측정 | 미측정 | 미측정 | 미측정 | 대기 |
| 비전 + SLAM + Nav2 + 음성 | 미측정 | 미측정 | 미측정 | 미측정 | 미측정 | 대기 |

측정은 바퀴를 띄운 정지 통합 시험을 먼저 통과한 뒤 실제 주행으로 진행한다. OOM뿐 아니라
최소 available RAM, swap, 온도, 전력과 열 스로틀링을 함께 기록한다.

### 33.13.6 공개 한국어 STT 기준선

이 수치는 현재 Qwen3-ASR이 아니라 **과거 로컬 Whisper small의 방법론·오류율 기준선**이다.
KsponSpeech 공식 평가셋에서 라틴 문자 포함 24개와 5자 미만 505개를 제외한 2,471발화를
clean/SNR 10/5/0dB, 프롬프트 있음/없음으로 총 19,768회 인식했다.

| 조건 | CER 프롬프트 있음 | CER 없음 | WER 프롬프트 있음 | WER 없음 |
|---|---:|---:|---:|---:|
| clean | 0.141 | **0.137** | 0.305 | **0.304** |
| SNR 10dB | 0.206 | **0.188** | 0.379 | **0.361** |
| SNR 5dB | 0.259 | **0.235** | 0.434 | **0.413** |
| SNR 0dB | 0.366 | **0.352** | 0.533 | **0.495** |

모든 조건에서 도메인 프라이밍이 손해였고 프롬프트 문장이 전사로 되돌아온 사례가 57회,
무프롬프트에서는 0회였다. 현재 운영 ASR 요청에 Whisper식 도메인 프라이밍을 넣지 않는
근거다. 이 코퍼스는 정상 화자의 일반 대화이므로 재난 현장·약한 발화 성능을 증명하지 않는다.

### 33.13.7 잡음 제거 A/B

실제 육성 6개와 모터·화재·현장음·사이렌 등 소음 6종을 합성한 108조건 결과다.

| 지표 | 원본+소음 | DeepFilterNet 후처리 |
|---|---:|---:|
| CER 승리 | **58** | 16 |
| 무승부 | 34 | 34 |
| CER 중앙값 | **0.467** | 0.667 |
| 환각 발생률 | **0.9%** | 13.9% |

DeepFilterNet의 x86 처리 비용은 60초 오디오 약 1.0초, RTF 0.017, 프로세스 전체 peak
약 1.0GB였다. EC2 실측 당시 available RAM 14,091MB, 디스크 여유 297GB로 관제
청취본 후처리 서버 자원은 충분했다.

실제 아이폰 동시 녹음 1건에서는 다음 결과가 나왔다.

| 구간 | 원본 | 제거 후 | 변화 |
|---|---:|---:|---:|
| 발화 구간 RMS | -26.9dB | -31.7dB | **-4.7dB** |
| 소음 구간 RMS | -31.8dB | -50.9dB | -19.1dB |
| 발화/소음 레벨차 | 4.9dB | 19.2dB | +14.3dB |
| 1~3.4kHz 음성대역 잔존 | 100% | 5.9% | 대부분 소실 |
| 3.4~8kHz 고역 잔존 | 100% | 0.2% | 대부분 소실 |

소음은 크게 줄었지만 음성 자음 성분도 제거돼 정확했던 원본 전사가 붕괴했다. 감쇠 상한을
6~30dB로 제한해도 원본 STT를 넘지 못했다. 따라서 관제 청취 편의에는 사용할 수 있지만
STT 입력에는 사용하지 않는다.

### 33.13.8 수치 사용 시 주의사항

- 합성 음성·합성 소음 결과를 실제 재난 현장 성능으로 표현하지 않는다.
- 과거 Whisper·로컬 LLM RAM을 현재 원격 Qwen 구성의 값으로 재사용하지 않는다.
- 평균만 보고하지 않고 P50, P95, 최대, 실패 건수와 표본 수를 함께 적는다.
- CER/WER와 핵심 슬롯 정확도는 다른 지표이므로 서로 대신하지 않는다.
- 실제 사람 음성, API 키, 원본 세션과 로컬 `results/`는 Git에 커밋하지 않는다.
- 현재 구조의 최종 수치는 Jetson 전체 스택과 BRIO 현장 코퍼스 측정 후 갱신한다.
