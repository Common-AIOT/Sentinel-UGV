# STT-GMS-사전녹음 안내 파이프라인 의존성
#
# ⚠️ Jetson(ARM64+CUDA)의 torch는 JetPack 전용 휠을 사용한다.
#
# 개발 PC(x86)에서는 아래를 그대로 pip install 해도 된다.

# 오디오 I/O·전처리
sounddevice
soundfile
numpy
scipy
librosa

# VAD (torch 의존)
silero-vad
# torch  ← Jetson은 NVIDIA 전용 휠로 별도 설치(여기서 pip install 금지)

# LLM 클라이언트 (GMS API — https://gms.ssafy.io)
openai                # GMS API 호출 (팀 결정 2026-07-24, 온디바이스 LLM 전환)
httpx                 # GPU ASR FastAPI 클라이언트

# 안내 음성은 승인된 PCM WAV를 재생하므로 TTS 모델 의존성이 없다.

# 개발·검증 전용 (젯슨 운영에는 불필요)
#
# tests/test_integration.py 는 pytest 기능(importorskip·raises·parametrize)을 쓴다.
# 없으면 `python -m unittest discover -s tests` 가 모듈 로드에 실패해 항상 에러 1건이
# 남는다. 상시 실패가 있으면 진짜 실패를 못 알아본다(S15P11A301-179).
pytest
jsonschema            # 공통 스키마 계약 검증
fastapi>=0.116,<1      # asr_server API 계약 테스트·서빙
python-multipart>=0.0.20,<1
httpx>=0.28,<1        # FastAPI TestClient
