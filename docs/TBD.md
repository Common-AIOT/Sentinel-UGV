> Sentinel UGV 통합 명세서 v1.1의 일부 — 문서 규칙·버전·변경 이력은 [README](README.md) 참조

# 부록 H. 최종 TBD 및 변경 관리 (단일 TBD 대장)

본 표는 **문서 전체에서 유일한 TBD 대장**이다. 본문에는 TBD ID 참조만 두고 별도 표를 만들지 않는다(6.7 참조). ID 체계는 구 6.7장 하드웨어 목록을 승계했다. 항목이 확정되면 확정값을 "결정 시 기록할 값" 열에 채우고, 선택 근거·측정값·결정일을 문서 변경 이력에 기록한다. 구현 중 명세와 다른 구조를 적용할 경우 실제 구현을 우선하되, 변경 이유와 장단점, 일정 영향, 대체안을 본 문서에 반영한다.

| **ID**      | **미확정 항목** | **결정 시 기록할 값**                           | **담당(역할)** | **기한** |
|-------------|-----------------|-------------------------------------------------|----------------|----------|
| TBD-HW-001  | RS540 구동계 실측 | **2026-07-30 모델·구성 확정**: 후륜 좌·우 각 1개씩 총 2개(RS540 FD-12V RPM14000, 개별 제어). **2026-08-01 차동 구동 전환**: 좌·우 속도 차로 전진·후진·조향을 모두 수행. **2026-08-06 전륜 서보 조향 전환으로 역할 축소 — 이 2개는 전진·후진만 담당한다**(34-2). 조향은 TBD-HW-008. 감속비, 바퀴 지름, 정지 전류, 목표 속도, 좌·우 편차는 계속 실측 진행. **선회 중 후륜 스크럽 정량화가 추가됐다** — 기계식 차동장치가 없어 내·외측 후륜이 노면을 문지르며, 심하면 전자 차동 보정을 검토한다(TBD-CAL-002) | 임베디드·기구 B | 1주차 초 |
| TBD-HW-002  | 모터 드라이버   | **2026-07-30 모델 확정**: BTS7960(좌 구동·우 구동·조향). **2026-08-01 차동 구동 전환으로 2개(좌 구동·우 구동)로 감축**. **2026-08-06 전륜 서보 조향 전환에도 2개를 유지한다** — 새 조향 액추에이터는 자체 드라이버를 내장한 서보이므로 BTS7960 3번째가 필요하지 않다. 채널당 연속/피크 전류 실측은 진행 | 임베디드·기구 B | 1주차 초 |
| TBD-HW-003  | 배터리·DC-DC·전원 | **2026-07-30 구성 확정**: 모터 전원(유아전동차 내장 12V 배터리)과 Jetson 전원(대용량 보조배터리)을 완전 분리(6.6·21.4). **2026-08-06 서보 레일 결정**: 조향 서보는 **12V 모터 계통의 E-Stop 하류**에서 직접 급전한다(저전압 DC-DC 레일에 올리지 않는다 — stall 전류가 커서 ESP32·센서 논리 전원이 흔들린다). 차단점을 하나로 유지해 배선·검증을 단순화하는 대가로, E-Stop 시 서보가 무여자되어 조향각이 정의되지 않는다(34-7). ESP32 2개·MT6701 2개 등 저전압 레일을 어느 계통에서 분기할지와 DC-DC 정확한 사양, **서보 stall 전류를 더한 피크 전력**, 예상 운용 시간은 계속 진행 | 임베디드·기구 B | 1주차 중 |
| TBD-HW-004  | 엔코더 PPR      | **2026-07-30 모델 확정**: MT6701 자기식 엔코더(좌 구동·우 구동·조향). **2026-08-01 차동 구동 전환으로 2개(좌 구동·우 구동)로 감축**. **2026-08-06 전륜 서보 조향 전환에도 2개를 유지한다** — 서보가 내부 폐루프로 각도를 유지하고 외부로 출력하지 않으므로 조향각 엔코더를 두지 않으며, 조향은 개루프로 운용한다(34-5·34-9). 출력 모드(I2C 절대각 vs ABZ 증분), 분해능, 장착점은 계속 진행 | 임베디드·기구 B | 1주차 중 |
| TBD-HW-005  | ~~짐벌 서보·IMU~~ | **2026-07-30 폐기 — 짐벌 프로젝트 제외 확정(6.4).** 카메라·LiDAR는 차체 고정 마운트로 대체하며 별도 서보·head IMU가 필요 없다 | — | 폐기 |
| TBD-HW-006  | 최종 크기·중량  | 3D 모델·부품 배치 완료 후 실측값                | 임베디드·기구 B | 2주차 초 |
| TBD-HW-007  | E-Stop 부품     | **2026-07-30 전원 구조 확정**: 12V 배터리(모터 전용) 단일 레일이 BTS7960 공통 전원을 급전(21.4). **2026-08-01 차동 구동 전환으로 공통 전원 대상은 BTS7960 2개**. **2026-08-06 차단 대상에 조향 서보 추가** — `BTS7960 ×2 + DS51150 서보`의 합산 최대 전류(구동 stall + 서보 stall 동시)를 감당해야 한다. 차단 정격·부품 모델은 계속 진행 | 임베디드·기구 B | 1주차 중 |
| TBD-HW-008  | **전륜 서보 조향 기하** | **2026-08-06 부활·구성 확정(6.3)**: 2026-08-01 에 폐기했던 항목이다. 캐스터 2개를 제거하고 **유아전동차 기존 조향 앞바퀴 2개를 복구**, 타이로드에 **DS51150-12V 서보모터**를 직결해 조향한다. 서보 제어는 모터 ESP32(PWM 1채널, 별도 LEDC 타이머). 프로토콜은 예약 필드를 그대로 되살려 쓰므로 **와이어 포맷 변경이 없다**(`target_steering_mdeg`·`max_steering_rate_mdps`·`steering_actuator_cmd`, 34-5). **남은 값이 이 항목의 본체다 — 이것들이 없으면 `vehicle_kinematics` 재작성과 Nav2 재구성을 검증할 수 없다**: ① 휠베이스 `L`(전·후 차축 간격), ② 최대 조향각 `δ_max`(좌·우 각각, 작은 쪽으로 대칭 클램프), ③ 최소 회전반경 `R_min` 실측(원주행, 계산값 `L/tanδ_max` 와 비교), ④ 서보 중립·좌우 엔드포인트 펄스폭(µs)과 5점 이상 각도 매핑 표, ⑤ 링키지 왕복 히스테리시스(유격), ⑥ 조향 변화율 상한, ⑦ 서보 신호 규격(표준 RC PWM 50Hz·500~2500µs 가정 — 데이터시트 확인)과 3.3V 논리 구동 가능 여부, ⑧ stall 전류. 절차는 35-3 「조향 중립·각도 매핑」·「휠베이스·최소 회전반경 실측」. **`R_min` 은 Nav2 planner 선택(NavFn 유지 vs Smac Hybrid-A\* 전환)의 판단 기준이기도 하다**(24.1) | 임베디드·기구 B | **즉시** |
| TBD-HW-009  | ESP32 제어 보드 | **2026-07-30 구성 확정**: ESP32 WROOM-32 2개로 이원화 — 모터 제어용(BTS7960 2개 + **2026-08-06 조향 서보 PWM 1채널** 연결) 1개, 센서 제어용(MT6701 2개·차체 IMU·DHT-11·HC-SR04 연결) 1개. 각각 Jetson과 독립 USB Serial 연결(`/dev/sentinel_mcu_motor`, `/dev/sentinel_mcu_sensor`). WROOM-32는 native USB CDC가 없어 온보드 USB-UART bridge 경유. **2026-08-04 브리지 칩 실측 확정: 두 보드 모두 CH340(`1a86:7523`)이고 시리얼 번호가 없다** — `udevadm info -a` 에 `ATTRS{serial}` 이 아예 나오지 않는다(라이다의 CP2102 는 `serial=="0001"` 이 있다). 그래서 by-id 별칭이 불가능해 USB 물리 경로로 고정한다(`devpath` 2.3=센서, 2.4=모터, S15P11A301-214). 이 결정은 두 곳의 판정 근거다 — `esp32_bridge.yaml` 의 기본 포트가 그 별칭이고, `demo_up.sh` 가 `/dev/sentinel_mcu_sensor` 존재로 `enable_esp32` 를 정한다(S15P11A301-256). 모터 보드 포트(2.4)는 dmesg 이력 추정이며 보드 복귀 후 확인이 남았다. **GPIO 핀 배정은 계속 진행** | 임베디드·기구 B | 즉시 |
| TBD-HW-010  | 전방·후방 초음파 센서 | **2026-07-30 모델 확정**: HC-SR04 1개(전방), 센서 제어용 ESP32에 연결(TRIG/ECHO, 5V→3.3V 레벨 변환 필요). 설치 위치·유효 거리·갱신률·실차 정지 임계값은 계속 진행. **2026-08-08 후방 추가 착수(S15P11A301-324)**: 전방과 **동일한 HC-SR04**를 채택해 부품 종류를 늘리지 않는다. 같은 센서 ESP32에 전방과 별도의 TRIG/ECHO 핀 쌍으로 연결하고, 두 센서를 동시에 trigger하면 서로의 echo를 오독하므로 순차 측정한다(02장 6.5·21.3). **남은 값**: ① 후방 장착 위치(범퍼·후륜 비간섭, 최대 조향각에서도 음장 확보), ② 후방 유효 거리·갱신률, ③ 후방 `protective_stop` 임계값(전방의 실측 0.15~0.18m·TBD-CAL-001을 그대로 가져오지 않고 별도 실측), ④ GPIO 핀 배정(부록 J), ⑤ 후방 거리를 담을 프로토콜 필드와 Jetson 중계 경로 확장 여부(03장 `PROXIMITY_STATE`, TBD-HW-011) | 임베디드·기구 B | 실차 자율주행 전 |
| TBD-HW-011  | 듀얼 ESP32 속도 폐루프·초음파 릴레이 위치 | **2026-08-08 후방 초음파 추가로 항목이 하나 늘었다**(TBD-HW-010, S15P11A301-324): 현재 `PROXIMITY_STATE`는 `front_min_distance_mm` 하나뿐이라 후방 거리를 실을 필드가 없다. 후방을 기존 프레임에 추가할지, 별도 프레임을 둘지, `esp32_motor_bridge_node`의 `/proximity/protective_stop` 구독을 전방·후방 통합 정지 신호로 유지할지 아니면 방향별로 분리할지는 후방 배선·캘리브레이션 이후 결정한다. MT6701 엔코더 2개가 센서 ESP32에, BTS7960 드라이버 2개가 모터 ESP32에 분리 연결되어 있고 두 ESP32 간 직접 배선이 없다(21.3). 좌·우 구동 속도 폐루프와 HC-SR04 근거리 보호 정지는 Jetson이 두 채널을 중계해 계산·실행하는 것으로 잠정 확정(34-2·34-5·34-8). USB 왕복 지연이 300ms watchdog·안전 목표를 해치면 ESP32 간 직접 UART 링크 추가를 대체안으로 검토. **2026-08-05 중계 위치 확정(S15P11A301-237)**: 보호 정지 중계는 `esp32_motor_bridge_node` 가 `/proximity/protective_stop` 을 직접 구독해 `STOP_COMMAND` 를 보낸다 — `safety_gate` 를 거치지 않는 이유는 지연(홉)과 독립성(게이트 생존에 안전 경로를 걸지 않는다)이다. 상승 엣지 3회(03장 메시지 표) + 눌린 동안 200ms마다 1회 재확인(ESP32 재부팅 복구용). 상세는 03장 「중계 구현」. **남은 값은 왕복 지연 실측 하나다** — `protective_stop` 발생부터 PWM 0 까지의 시간이며 두 보드가 붙은 실차에서만 잴 수 있다(CTRL-18). 그 값이 300ms 를 넘으면 위 UART 대체안을 꺼낸다. **2026-08-06 수동 조종 도입으로 결정 시 기록할 값이 하나 늘었다**(S15P11A301-298): 수동 주행 중 초음파 보호 정지가 걸렸을 때 **누름당 실제 주행 시간**이다. 중계는 200ms 마다 `STOP_COMMAND` 를 재전송하고 ESP 는 keep-alive 와 새 운영자 정지를 구분할 수 없으므로, 보호 정지가 연속 유지되는 동안 운영자는 누름당 ≤200ms 만 얻는다 — 정지 구역에서 후진으로 빠져나오기에는 충분하지만 UX 는 나쁘다(CTRL-33). 이것이 **ESP32 간 직접 UART 링크의 두 번째 논거**다: 직결이면 모터 보드가 스스로 거리를 보므로 중계 keep-alive 자체가 사라진다. UART 를 채택하지 않는다면 `protective_relay` 가 `MANUAL_ACTIVE` 동안 keep-alive 를 억제하도록 고친다 | 임베디드·기구 B | 2주차 초 |
| TBD-HW-012  | 차체 IMU 모델·배선·장착 오프셋 | **2026-08-04 모델·배선 확정(S15P11A301-244)**: MPU6050, I2C `0x68`(AD0=GND), MT6701 엔코더와 GPIO21/22 버스 공유, ±500dps·±4g·DLPF 44Hz, 100Hz 샘플(젯슨 수신 90Hz 실측). `IMU_STATE`(0x26) 프레임으로 원시 gyro·accel 전달, 부팅 시 2초 bias 보정. **2026-08-08 장착 방향·오프셋 실측 완료(S15P11A301-344)**: 위치 (0.233, −0.010, 0.215)m + 장착 기울기 pitch +9.3°·roll −2.3° (브레드보드 마운트 경사 — 정지 가속도 (−1.65, −0.40, +10.04)에서 역산, 90° 회전 후에도 동일해 바닥 경사 배제, 육안 확인). 축 매핑은 기능 시험(앞들기→ax+·왼쪽들기→ay+·반시계→gz+, 적분 +92°/실제 90°)으로 **REP-103 그대로 확인** — 펌웨어 `IMU_AXIS_SOURCE`/`SIGN` 항등 유지가 맞다. URDF `base_link→imu_link` 에 반영됨. 잔여 gyro bias −4.85°/분 실측(EKF 가 bias 를 추정하지 못하므로 yaw 표류로 남는다). **2026-07-30 연결 구조 확정**: 차체 IMU 1개를 센서 ESP32의 I2C 또는 SPI에 연결하고, ESP32 측정 시각·sequence와 원시 gyro·accel을 USB Serial로 Jetson에 전달한다. Jetson은 `/imu/data_raw` 발행과 wheel odometry EKF 융합을 담당한다. 남은 값은 IMU 모델, I2C/SPI 선택, bus 주소·GPIO, 전원, 장착 X/Y/Z·RPY, bias·진동 실측이다 | 임베디드·ROS2/주행 A | 실차 EKF 연동 전 |
| TBD-CAM-001 | BRIO 100 ROS 2 연동 | 720p MJPEG 캡처와 X4 Pro 동시 구동 안정성      | AI A           | 개발 첫날 |
| TBD-SW-001  | ~~추가 학습 YOLO26n 배포~~ → **2026-08-06 해소.** 파인튜닝 미채택으로 데이터셋 버전 관리 항목이 사라졌다(07-AI-탐지 25.4). 배포 대상은 사전학습 `yolo26n`·`yolo26n-pose` 가중치이며 해시와 라이선스는 06장 릴리스 항목에서 관리한다. **남은 것은 TensorRT engine 변환 후 FPS 실측 하나다** | AI A | 해소 |
| TBD-NAV-001 | Frontier 구현   | 패키지 또는 자체 노드, 파라미터. **2026-08-04: Nav2 채택 방향 확정** — 젯슨에 `navigation2 1.1.20`·`robot_localization 3.5.4`가 이미 설치돼 있고, 명세가 Nav2를 하중 받는 자리 세 곳(23.3 도달성 판정, 23.4 정체 복구, 23.5 복귀 목표)에 쓰고 있다. **2026-08-04 구성 확정(S15P11A301-235)**: planner NavFn, controller rotation shim → Regulated Pure Pursuit, `allow_unknown=false`(23.3 도달성 판정의 근거), costmap 2개(global=map/static, local=odom/scan), cmd_vel 은 `/cmd_vel_nav`(안전 체인 입력). 파라미터는 `sentinel_bringup/config/nav2.yaml`, 상세 근거는 24.1 「구성 확정」. 축소판(순찰 시퀀스) 판단만 S15P11A301-172 에 남는다. **2026-08-06 전륜 서보 조향 전환으로 일부 재검토가 열렸다**(24.1 「전륜 조향 전환의 Nav2 영향」): rotation shim **제거**, `behavior_server` 의 `spin` **비활성**, `backup` 은 저속·거리 제한(후방 초음파 없음), RPP `use_rotate_to_heading: false`, `minimum_turning_radius` 신설. **2026-08-08 후방 초음파 하드웨어 추가 착수(S15P11A301-324, TBD-HW-010)** — 배선·캘리브레이션·안전 체인 통합이 끝나기 전까지는 `backup`의 "후방 초음파 없음" 제약을 그대로 유지한다. 통합이 끝나면 이 제약 완화(거리 상한 확대 등) 여부를 재검토한다. **2026-08-09 planner 확정(S15P11A301-358): Smac Hybrid-A\* 전환.** 판단 기준이던 `R_min` 실측이 들어왔고(341: 좌 1.37m·우 1.76m), 두 실기동에서 NavFn 이 회전반경을 모른 채 후방·측면 목표로 못 따라갈 경로를 내는 대가가 그대로 관측됐다(조향 상한 고착, 좌우 진동 — r60·r61). `SmacPlannerHybrid` + REEDS_SHEPP(`minimum_turning_radius 1.8`) + RPP `allow_reversing` 구성. **후진 계획 동작 확인**(계획 실측: 비용 0 회랑 정후방 1.1m → 전원 후진 포즈의 일직선 경로). 한 차례 「후진 불가」로 오판했다가 정정 — 시험 목표가 costmap 벽 팽창 안이었다. 남은 검증은 실주행(cusp 방향전환 데드타임 × RPP). 후진 도입으로 collision_monitor 정지·감속 구역도 후방으로 확장했다(`safety.yaml`). RPP·`allow_unknown=false`(23.3 결합 유지)·costmap 구성·remap 은 그대로 유효하다 | ROS2·주행 A    | 2주차 |
| TBD-AUD-001 | 오디오 입출력 검증 | 스피커는 블루투스(A2DP) **확정**, 마이크는 차체 고정 카메라(BRIO 100) 내장 **잠정** — 주행 소음 환경 STT 인식률, 미달 시 USB 마이크 전환 | AI B | 1주차 |
| TBD-AUD-002 | 음성 모델 선정 | **2026-08-05 확정**: STT `Qwen3-ASR-1.7B`(L40S FastAPI), LLM `gpt-5.4-mini`(GMS 구조화 전용), 안내는 승인된 사전 녹음 WAV. Jetson에는 Silero VAD와 클라이언트만 둔다. 남은 값은 실제 BRIO 소음 코퍼스의 오류율과 전체 스택 E2E·자원 실측 | AI B | 소음 시험 |
| TBD-AUD-003 | 음성 세션·Mission Manager ROS 2 연동 계약 | 대화 상태 토픽·서비스, 메시지 스키마, `ENDED` 조건, 탐사 재개 요청·응답, 실행 패키지 경계. 현재 계약은 `08-AI-음성.md` 33.4·33.6에 기록 | AI B·ROS2/주행 A | Jira 117 통합 전 |
| TBD-CAL-001 | 최종 주행값     | 좌·우 구동 PID, **조향각↔실제 yaw rate 매핑**, 최대 각속도, **좌·우 선회 `R_min` 대칭**, 속도·가속도, 정지거리, person confidence 최종값. **2026-08-06 전륜 서보 조향 전환으로 「제자리 회전 대칭」 항목을 폐기하고 「좌·우 선회 대칭」으로 대체했다**(제자리 회전 불가, 6.3). **2026-08-05 초음파 임계 확정(S15P11A301-272)**: 실측 **0.15~0.18m** 이며 **바꾸지 않는다** — 0.43m 에서 탐지율이 이미 20~45%로 떨어져(1.4m 는 7~27%) 임계를 올리면 놓치는 비율이 그대로 오판이 된다. 역할을 접촉 직전 방어로 좁히고 주 방어는 LiDAR·collision_monitor(전방 0.40m)가 맡는 것으로 정리했다. 거리별 탐지율 실측표는 04장 22.2. **2026-08-08 후방 초음파 추가(TBD-HW-010, S15P11A301-324)로 이 임계값이 전방 전용임이 명확해졌다** — 후방 센서의 `protective_stop` 임계는 장착 각도·높이가 다르므로 0.15~0.18m를 그대로 쓰지 않고 후방 배선 완료 후 별도로 실측한다. **2026-08-09 IMU·EKF 정확도 확정(S15P11A301-363)**: 손으로 90° 회전시켜 EKF 측정 **89.02°(오차 1.1%)**, 정지 시 자이로 바이어스 **+0.014°/s(분당 0.9°)**, 정지 12초 EKF yaw 드리프트 0.14°. 이 값으로 「조향각↔실제 yaw rate 매핑」의 관측측 신뢰도가 확보됐고, SLAM 스캔 매칭 탐색 범위를 좁힐 근거가 됐다(초기 추정이 정확하면 넓은 탐색이 오히려 해롭다). **남은 값은 정지거리 등 주행 항목과 후방 초음파 임계 실측이다** | ROS2·주행 A | 3주차 |
| TBD-CAL-002 | **바퀴 지름·후륜 트랙폭 `W`** | **2026-08-04 신설(CAL-001에서 분리).** 바퀴 지름이 없으면 `vehicle_kinematics`(S15P11A301-234)의 지령을 실제 속도로 환산할 수 없다. 좌·우 거리 스케일도 함께 잰다. **2026-08-06 전륜 서보 조향 전환으로 `W` 의 역할이 바뀌었다** — 차동 역운동학의 필수 파라미터에서 빠졌고(그 자리는 휠베이스 `L`, TBD-HW-008이 받는다), 이제는 ① 선회 중 후륜 스크럽 정량화와 ② 전자 차동 보정을 도입할지 판단하는 데 쓴다. **전자 차동은 기본 미적용으로 확정했다**(6.3 — 초기에는 좌·우 동일 속도를 주고, 조향 중 타이어 스크럽·편향을 실측한 뒤 필요하면 추가한다). 즉 `W` 는 여전히 재지만 이제 주행을 막는 값은 아니다 | 임베디드·기구 B | 1주차 중 |
| TBD-VID-001 | 영상 저장 매체  | **2026-07-28 확정**: NVMe 미장착 상태로 진행하고, 링 버퍼와 `pending`을 microSD 단일 볼륨에 둔다(부록 D 경로). `pending` 보존 상한은 30분 분량(약 560MB)이며 초과 시 업로드 완료분부터 삭제한다(32-5). 링 버퍼는 8초 순환이라 누적하지 않으므로(약 2.5MB 고정) 용량 위험은 `pending`에만 있다. 남은 제약은 용량이 아니라 microSD 쓰기 내구성이며, 시연 범위를 넘는 상시 녹화는 NVMe 장착을 전제로 한다 | 백엔드·영상 A | 확정 |
| TBD-VID-002 | 목표 비트레이트 | PoC-B x264 CPU 실측 후 확정. 초기값 2.5 Mbps, 미달 시 해상도·FPS 축소 순서 적용(32-8) | 백엔드·영상 A | 1주차 말 |
| TBD-OPS-001 | systemd 서비스화 | **2026-08-04 정정: 유닛 1개 구현됨** — `scripts/systemd/sentinel-demo.service`(S15P11A301-156), 기본 비활성. 37-3의 9개로 쪼개는 구조는 채택하지 않고 단일 진입점(`demo_up.sh` → `demo.launch.py`)으로 갔다. **2026-07-28 MVP 범위 외로 결정.** MVP가 자동 복구를 구현하지 않으므로 `Restart` 정책의 이점이 사라지고 남는 것은 부팅 시 자동 시작 편의뿐이다. 개발 중에는 `journalctl`과 `Restart`가 오히려 디버깅을 방해한다. **2026-08-06 추가(S15P11A301-294): 그 방해가 실제로 발생했다** — 시연 준비로 유닛을 `enable`한 뒤 `demo_down.sh`로 내렸는데 `Restart=on-failure`가 5초 뒤 스택을 되살렸다(launch는 SIGINT에 exit 1로 끝나므로 systemd가 고장으로 읽는다). `demo_down.sh`가 유닛 상태를 보고 `systemctl stop`을 타도록 고쳤다. 서비스를 끈 개발 구성에서는 실행을 `scripts/demo_up.sh`로 사람이 한다. 38장 인수 시험에 systemd를 요구하는 항목이 없고, 안전은 ESP32 watchdog이 담보하므로 안전 영향도 없다. 도입한다면 시연 리허설 전에 `sentinel-ros-bringup`과 `sentinel-media` 둘만 만든다. 없는 노드의 유닛을 미리 만들면 S15P11A301-125에서 지운 빈 CI 스테이지와 같은 함정이 된다. **2026-08-06 추가(S15P11A301-306): `enable` 해 둘 때의 대가가 하나 더 드러났다** — 이 젯슨은 RTC 백업 배터리가 없어 부팅마다 시계가 두 번 점프하고(epoch → 마지막 기록 시각 → NTP 실동기), 유닛이 그 사이에 뜨면 두 번째 점프가 TF 캐시·스트림 타임아웃·ESP32 워치독을 한꺼번에 부순다. 유닛을 `time-sync.target` 뒤로 보내고 `systemd-time-wait-sync` 를 `enable` + `TimeoutStartSec` 상한을 걸어 막았다(06장 37-3-1). 손으로 `demo_up.sh` 를 부르는 개발 구성에는 이 문제가 없다 — 그때는 시각이 이미 맞아 있다 | 백엔드·영상 A | MVP 이후 |
